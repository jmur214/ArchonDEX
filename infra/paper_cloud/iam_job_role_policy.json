{
  "_comment": "Permissions for the paper-loop CONTAINER (jobRoleArn): durable-state S3 read/write under paper_state/* and CloudWatch put-metric-data for the dead-man's-switch. Deliberately NOT granted broker creds (those come via the execution role's Secrets Manager binding) and NOT granted any live-trading AWS surface. __RESULTS_BUCKET__ substituted at deploy.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PaperStateRW",
      "Effect": "Allow",
      "Action": ["s3:GetObject", "s3:PutObject"],
      "Resource": "arn:aws:s3:::__RESULTS_BUCKET__/paper_state/*"
    },
    {
      "Sid": "PaperStateList",
      "Effect": "Allow",
      "Action": "s3:ListBucket",
      "Resource": "arn:aws:s3:::__RESULTS_BUCKET__",
      "Condition": {"StringLike": {"s3:prefix": "paper_state/*"}}
    },
    {
      "Sid": "DeadMansSwitchMetrics",
      "Effect": "Allow",
      "Action": "cloudwatch:PutMetricData",
      "Resource": "*",
      "Condition": {"StringEquals": {"cloudwatch:namespace": "ArchonDEX/PaperLoop"}}
    }
  ]
}
