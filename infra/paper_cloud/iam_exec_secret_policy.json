{
  "_comment": "Attached to the ECS task EXECUTION role: lets Fargate's agent read the Alpaca PAPER secret to inject it as env into the container. Scoped to the one secret ARN (__SECRET_ARN__). The standard AmazonECSTaskExecutionRolePolicy (ECR pull + CloudWatch Logs) is attached separately.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ReadAlpacaPaperSecret",
      "Effect": "Allow",
      "Action": "secretsmanager:GetSecretValue",
      "Resource": "__SECRET_ARN__"
    }
  ]
}
