# Session Summary: 2026-06-05 → 2026-06-10 (multi-day director arc, T-109→T-127)

> Covers the 4-agent coordination arc: research-driven two-track pivot, the alpha
> negatives, and the reproducibility crisis + reversal. Written 2026-06-10 while
> holding for B's T-127 (data-substrate pin).

## What was worked on

- **Scaled the coordination model 2→4 agents** (A=portfolio/sleeve, B=cloud/infra,
  C=risk-engine, D=alpha/edge) with automation first: outbox watcher
  (`scripts/watch_coordination.py`) + the T-114 no-TASK_LEDGER-write protocol
  (director writes ledger rows at merge; kills the N-way merge conflict).
- **External research synthesis** (docs/Sessions/Other-dev-opinion/6-6-26_*) →
  filed as the 2026-06-06 forward_plan block: two-track plan (de-gross overlay +
  literature edges), PARK Engine-D autonomous Discovery, Moreira-Muir look-ahead
  hard constraint.
- **The de-gross plumbing completed**: T-116 (HMM risk_scalar Path-A lift, sibling
  of T-111) + T-118 (HMM transition-trigger overlay, inert code merged, 52-cell
  pre-registered campaign armed; STEP-1 proved crisis-model+OFF == baseline canon).
- **Alpha-track falsifications**: T-117 (edge compression REFUTED — can't recombine
  factor-losers), T-122 (VRP ZERO α; equity-cross-sectional harness washes out
  timing signals — architecture is the constraint), T-123 (BAB ~0 α but
  INCONCLUSIVE — tested on its worst window, fair deep test queued).
- **Sleeve reversal**: T-120/T-121 — T-115's "spot @25% winner" does NOT survive
  faithful integration (engine-side capital-scale-dependence ~2.7pp/yr; the
  analytical model's scale-invariance assumption is violated). My "decouple"
  prescription was a misdiagnosis — A caught it; the formula was NOT merged.
- **The reproducibility crisis**: T-109 (image refresh) → found determinism 2/3 +
  baseline 0.246→0.446. T-125 (digest pin) → 3/3 restored but "confirmed" 0.446 as
  code-driven. **T-126 OVERTURNED that**: fresh build 3 days later reproduces
  0.246 to 3dp — the 0.446 was DATA-DRIFT (`Dockerfile.backtest:105-106` bakes
  data/processed+data/raw from the HOST, unpinned). T-127 dispatched (pin the data
  substrate + forensics).

## What was decided

- **T-092's 0.246 STANDS; the bull-conditional collapse is REAL** (no softening —
  the 0.446 was a mirage). The pivot premise holds.
- **Drawdown-reduction reframed as legitimate risk-adjusted edge** (geometric
  return/Calmar/survivability), per user pushback — not "just MDD".
- **The 7 dead Engine-B defensive controls are ORPHANED, not failed** (Path A +
  "ATR default" comment born in the same commit e96a6ea; none ever marked refuted).
- **Model fork for T-118**: drive the overlay with the CRISIS model (the validated
  0.914-AUC signal), not production v1 (the falsified AUC-0.49 one). Verified safe:
  arm0 is model-invariant (HMM invisible to Path A at overlay-OFF) — C confirmed
  empirically (canon bitwise) + in code.
- **Lean 50-cell grid over full 74** for the overlay campaign (lower N_trials
  penalty = lower deflated-Sharpe bar; densify only around a winner).
- **Held cloud wave** (T-118 overlay / sleeve A/B / T-113 / fair BAB) stays HELD
  until the data substrate is pinned (T-127). Don't run definitive A/Bs on
  shifting sand.
- Static-20 sector cap REJECTED (2024-luck); spot-sleeve prior now negative.

## What was learned

- **Reproducibility = base digest + lib lock + DATA substrate, all pinned.**
  Within-build 3/3 determinism ≠ cross-build reproducibility. A sudden ~80%
  baseline "improvement" should be presumed artifact, not good news.
- **Single-cell OFF-canon-identical ≠ 26-yr inert** — the "inert" merges moved the
  26-yr by a benign ~0.009. Future inertness proofs need a deep-window canon check.
- **The architecture is the alpha constraint** (T-117+122+123): the
  equity-cross-sectional harness only tests stock-picker signals; timing/overlay
  edges wash out (Gate-1 +0.000); options-class premia (true VRP) need instruments
  we don't have. "Implement more literature edges" must mean cross-sectional ones.
- **Analytical sleeve A/Bs overstate**: capital partitions are not scale-invariant
  in this engine (integer shares, min_notional, caps) — integrate before believing.
- Suspected data-drift source: all agent worktrees symlink ONE shared `data/`
  (except governor) — any task writing into it mutates the canonical substrate
  for everyone (T-127 forensics to confirm + make write-safe).

## State at session end

- **In flight:** B on T-127 (data pin + forensics) = THE GATE. C armed (52-cell
  overlay campaign launch-ready), A/D idle-held pending the gate.
- **Merged through:** 0ab1ec4 (T-126). CURRENT_STATE reconciled (0.246 stands;
  T-127 critical path).
- **User-action open:** AWS_ROLE_TO_ASSUME repo secret + IAM/OIDC (CI auto-rebuild
  dead until then; manual rebuild works). Steps in T-109 audit Part A.
- **Next on the gate clearing:** launch C's overlay campaign (the de-gross verdict,
  which matters MORE now the collapse is confirmed real) → sleeve A/B → T-113 →
  fair BAB deep-window re-test (the honest alpha referendum).
