# Session 2026-05-31 — Director summary (full-day arc; supersedes the T-088 worktree stub)

> **Handoff for the outside dev: this is "where we are."** Read this + `docs/State/CURRENT_STATE.md` (live dashboard) + `docs/State/TASK_LEDGER.md` (T-035→ row ledger). The big open item is the **T-092 Path A/B decision** (bottom). All branches referenced are pushed.

## TL;DR

A very large day. Engineering ratchet moved a lot; the alpha verdict landed **honestly negative-leaning**: the base 6-edge ensemble does **not** formally validate on any rigorous window, and the deepest window (26-yr) fails outright. Two external research analyses arrived and largely **confirm the project's own pessimism**. Net posture: **stop tuning the base, pivot to crisis-regime robustness / new alpha source** — pending one user decision.

## What shipped to main (all pushed)

| Task | Verdict | Note |
|---|---|---|
| **T-088** risk-config keyfix | done | `risk_per_trade_pct` confirmed a **DEAD KNOB** on prod (Path A uses `target_weight`; the knob lives only on never-run Path B). 5× key change → bitwise-identical trades. Historical verdicts STAND. Filter hardened to warn on unknown keys. |
| **T-090 / T-091** contract-test suite | done | 10 parametric tests <1s; silent-mismatch bug family now **structurally unmergeable** (CI-gated). Caught 3 known + 7 new null-read sites. |
| **2026-05-31 external research** (filed) | — | `docs/Sources/Research_2026_05_31/` — 2 prompts + 2 analyst findings + director synthesis README. Deployment context confirmed: **BOTH taxable-IL AND Roth** live. |
| **T-093** doc-system Phase 1 | done | `CURRENT_STATE.md` + `TASK_LEDGER.md` + `scripts/doc_lint.py` (7 anti-rot checks, pre-commit + CI) + `docs/Audit/README.md`. |
| **T-096** doc-system Phase 2 | done | SessionStart/Stop hooks externalized to **fail-open** scripts (`.claude/hooks/`); CLAUDE.md reading-order + supersession + forward_plan-vs-CURRENT_STATE rules; `docs/Core/NON_NEGOTIABLES.md` (duplicate-pointer — all 15 hard rules stay verbatim in CLAUDE.md). Design+verify ran as a workflow first; it caught a Stop-hook brick risk (a blocked Stop has no user force-end) and the @import-isn't-auto-loaded trap. |
| **T-095** fill-convention (H-Convention) | REFUTED (good) | The backtest **already fills at t+1 OPEN**, not close-to-close. Lou-Polk-Skouras overnight-alpha-leak does NOT apply; the ~0.81 baseline was never fill-inflated. Director-verified the citation. |

## On branches, NOT merged (refuted / decision-gated)

| Task | Branch @ commit | Verdict |
|---|---|---|
| **T-092** deep-substrate baseline | `feature/deep-substrate-baseline-t092` @ 15bdd97 | **PIVOT SIGNAL** (see below). Audit doc on main. |
| **T-098** H-Band no-trade bands | `feature/h-band-no-trade-t098` @ bf9da13 | **REFUTED** at ±20/25%. Clean default-OFF Engine C impl kept for a future tighter-band sweep. Audit docs on main. |

Both branches' audit docs were brought onto main this session; the code stays on-branch (refuted/inert, decision-gated). Merge whenever — no urgency.

## The headline result — T-092 (the load-bearing finding)

**Sharpe INVERTS with window depth:** 5yr 0.60 → 12yr 0.81 → **16yr 1.018** → **26yr 0.246**.

| Window | Sharpe | ci_low | CAGR | MDD | MBL | point-DSR | ci_low>DSR-bench (0.66) |
|---|---:|---:|---:|---:|:-:|:-:|:-:|
| 16yr (2010-25, n=5) | 1.018 | 0.560 | 11.0% | -15.4% | ✓ | ✓ | ✗ |
| 26yr (2000-25, n=4) | 0.246 | **-0.119** | 2.64% | -59.3% | ✗ | ✗ | ✗ |

- **16-yr is the strongest cell ever measured** — but it's the window that *excludes* 2008 + the 2000-02 dotcom bust. Anointing it is specification search.
- **26-yr fails every gate**: 8/26 years negative, worst 2008 (-1.28), cumulative underperforms buy-and-hold SPY by ~4%/yr.
- **The strict `ci_low > deflated-DSR-benchmark` (0.66) gate fails on EVERY window** (12yr 0.33, 16yr 0.56). By CLAUDE.md #6, **nothing is formally validated.**
- **Survivor-only universe** → all numbers are UPPER bounds; the true figures are worse.
- **New methodology concern A surfaced:** determinism drift scales with window depth (12yr ≤0.1, 16yr 0.065, 26yr 0.19, with >3-Sharpe per-year swings between canonical and drift cells). T-057c-det-followup is **not sufficient at long windows** — long-window measurement reliability itself needs a fix regardless of Path A/B.

## What the external research said (converges with the above)

Two independent analysts (no codebase access). They **validate** the project's empirical pessimism — gradual vol-targeting fails OOS (we found this in T-055), 0.81 is the *top* of the honest retail band, DSR/MBL exceeded at N≈260. New load-bearing items:
1. **Fill convention** — tested as T-095, REFUTED (we already fill honestly). ✓ crossed off.
2. **Construction is structurally SHORT skew** (rebalance + inverse-vol + cap) vs the tail-capture objective — the sharpest *structural* critique. (No-trade bands tested as T-098 = REFUTED; a literal trend/barbell overlay is a different, untested lever.)
3. **Taxes** (H-Tax, staged, not run) — recompute after-tax frontier at true IL rates to sort strategies taxable-eligible vs Roth-only.

## OPEN — needs the user (and dev input welcome): T-092 Path A vs Path B

- **Path A** — treat 16-yr as the validation substrate, green-light overlay work (vol-target / confidence-gate / T-088 param sweep on a 16-yr A/B).
- **Path B** — treat the 26-yr collapse as a pivot-signal: the base is bull-conditional, so build crisis-regime robustness FIRST — an **HMM-gated binary kill-switch** on T-087's validated `hmm_p_crisis` (AUC 0.887, the one signal that IS validated-predictive), **Engine D gene-encoding unblock**, or the parked **LLM-analyst** path.

**Director read (not user-ratified): leans Path B.** A's own recommendation is Path B. The rigorous case: best-window selection = spec search; strict CI gate fails everywhere; survivorship makes it upper-bound; the 26-yr now fails ci_low>0 outright. Path A is defensible only as "accept 16-yr with documented caveats to keep momentum."

## Staged / queued

- **H-Tax (T-097)** — after-tax recompute at IL rates; staged in `/tmp/staged_briefs_htax_hband.md`, path-verified, not dispatched.
- **Statistical-enforcement-hooks workstream** — user chose "scope later"; backlog.
- **Long-window determinism fix** — extend T-057c-det per A's drift finding (new, surfaced by T-092).
