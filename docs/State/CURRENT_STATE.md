# CURRENT_STATE — ArchonDEX

**Last reconciled with source docs:** 2026-06-25 (🏁 **T-215 — THE FIRST HONEST BEAT-THE-ROBO VERDICT IS IN: H0 = FAILURE.** Matched A/B (base vs C/T-211 composition) on the honest PIT × realistic-cost substrate, 26yr, after-tax Roth, census-canonical, N=3: base Sharpe **0.119** (levered) / composition **0.020** — the composition is more defensive (overlay de-grosses in every crisis) but doesn't beat the base, and **neither approaches the robo** (both ci_low NEGATIVE, CAGR ~0). **Money stays in the Schwab robo.** The 0.751 "base" is SUPERSEDED as survivorship-inflated static-109; the honest deployable base ≈ 0.1–0.3. See the T-215 block below. — Prior, 2026-06-18: 🔁 **THE PIVOT — goal RESOLVED to "beat the Schwab robo" (not academic alpha); the re-architecture plan is executing (`re_architecture_plan_2026_06_18.md`), 5 fronts in flight (C re-aims the gate; E trend overlay + the 6/22 fill; A defensive screens; B risk-model/vol-target designs; D PIT de-bias). Both external reviews drove it.** Earlier today: 🎯 **THE SWING QUESTION ANSWERED — T-196 H0: the single-gene Foundry vocabulary has NO alpha; 0/35 cleared even at the lenient n=1. The cheap alpha hunt is EXHAUSTED → strategic fork (see Next decision). Measurement-integrity arc COMPLETE (T-199 closed the last canon-path swallow, anchors confirmed clean). E landing the first real fill via CLS today (OPG canceled).** Prior: paper go-live PRE-FLIGHT caught a real STOP → **schedule stays DISABLED; T-198 dispatched (held-position reconcile fix); SNS confirmed.** Also: T-194 loader-HALT + T-195 foundry-eval-harness-fix MERGED → **the swing question is now blocked ONLY on compute: B/T-196 builds the pre-authorized cloud-discover path to run the valid foundry test at scale; D/T-197 ports the 2 production fixes (incl. the gate1-cache fail-open D caught — the recurring silent-fail-open disease, again). T-175 structurally closed.** Prior context: T-174/175/176/178 merged + T-171/173 corrected — **the 2026-06-16 batch: the ALPHA HUNT REOPENS (Discovery was structurally broken by a 1-line bug, T-177→T-179) and the CRISIS-DEFENSE arc CLOSES (regime timing dead T-178; always-on 20% bought-MF is the ceiling; safe_f 0.928)**. VRP refuted (T-174). The 0.751 anchors ran SIMFIN-BLIND (T-175) → true base plausibly higher, re-run dispatched (T-180). Combined-candidate scorecard shipped (T-176). **T-179 + T-181 MERGED (2026-06-16/17): the alpha lane is UNBLOCKED (Discovery genes-inert fix proven — 32 composites 20-live/0-error) and the Phase-1 EXECUTION-CENSUS is LIVE (every backtest self-announces; 2 measurement-integrity CLAUDE.md non-negotiables codified).** FORK RESOLVED (user 2026-06-15, capability-first; Schwab robo = real-money home + benchmark). **T-180-v2 (2026-06-17): TRUE 21-edge re-anchor done (census-canonical) — the value/accruals edges are neutral-to-negative, the "true base is higher" hope REFUTED, base CONFIRMED borderline (26yr 0.751/ci_low 0.371; 16yr 1.105/0.625; nothing strictly clears). Paper loop is LIVE on cloud (E provisioned + verified end-to-end; dead-man's-switch proven on a real failure; first real fill queued; schedule DISABLED pending user-enable). The forward alpha lever = D's `--discover`, which RAN but is INCONCLUSIVE (the local harness is mis-wired — Gate-0 MBL on the wrong window + a degenerate baseline; T-195 fixes it) → the Foundry-vocabulary alpha verdict remains the OPEN swing question.**)

**✅ THE SUBSTRATE SAGA IS CLOSED (2026-06-14). THE LOTTERY IS DEAD (proven), and the "26yr collapse" was a SUBSTRATE ARTIFACT.** B's deterministic cov (fixed-order einsum replacing pandas' OpenBLAS gemm) makes Sigma byte-identical across tasks → all 3 windows are **N=5 bitwise-unanimous** (cov-pin PROVEN; no N=11/quantization tax). Four degradations behind the old cloud anchors are ALL fixed: the placement lottery (cov-pin), regime-blind macro (`data/macro` baked, T-167), truncated-SPY/regime (T-167), and the `_normalize_df` load-truncation that ran the "26yr" on a ~19-ticker universe since T-082 (T-167) — plus a recent mean_variance os-crash (T-140-fu2's own `import os` shadow, swallowed by a broad except → the silent 0-trades; T-167). The 26yr WINDOW was always real (calendar = ticker-union, not SPY).

**⛔ SUPERSEDED AS "THE BASE" (T-215, 2026-06-25): the 0.751 anchor below ran on the
STATIC-109 universe (`n_resolved=109`, survivorship-biased), NOT the PIT survivor-bias-free
universe. The FIRST direct measurement on the honest PIT × realistic-cost substrate (T-215,
26yr, census-canonical) is Sharpe ≈ 0.1–0.3 (0.119 measured but LEVERED to 3.48× via Engine-B
per-name sizing; Sharpe is ~leverage-invariant). So 0.751 = survivorship-INFLATED static-109
(do NOT quote as the honest base); the honest deployable base is ≈ 0.1–0.3. See the T-215
block immediately below + the matched-A/B audit.**

**📌 THE 21-EDGE STATIC-109 ANCHORS (T-180-v2, CENSUS-CANONICAL: `fundamentals_blind=0`, N=5; image `sha-fc5a69e`) — survivor-biased UPPER bounds, NOT the honest base (T-215 supersedes):**

| Window | Sharpe | ci_low | MDD | universe |
|---|---|---|---|---|
| 2022 | 0.986 | (wide) | ~−22% | static-109 |
| 16yr (2010–25) | 1.105 | **0.625** | ~−16% | static-109 |
| 26yr (2000–25) | **0.751** | **0.371** | −33% | **static-109 (survivorship-inflated)** |

*(The 4 value/accruals edges that had been simfin-blind are **NEUTRAL-TO-NEGATIVE**: adding them back gave 2022 1.512→0.986, 16yr 1.162→1.105, 26yr UNCHANGED 0.751. **The "true base is higher" hypothesis (T-175's local-2022 Δ+0.33) is REFUTED.** Per-window canon md5s + exact CAGR/MDD in B's T-180 audit.)*

**🎯 T-215 — THE FIRST HONEST FULL-CYCLE BEAT-THE-ROBO VERDICT (2026-06-25): H0 = FAILURE TO HIT THE GOAL.** The matched A/B (base vs C/T-211 Phase-1 composition) on the **honest PIT × realistic-cost** substrate, 26yr, after-tax Roth, census-canonical, N=3 (metrics bit-identical; image `:sha-3863ef8`). Median panel:

| | BASE (PIT×rc) | COMPOSITION | robo bar |
|---|---|---|---|
| Sharpe / ci_low | **0.119 / −0.225** | **0.020 / −0.310** | ~0.4–0.6 / >0 |
| CAGR | +0.44% | **−0.40%** | ~5–6% |
| Total Return (26yr) | +12% | **−10%** | ~+250–300% |
| MaxDD | −63.5% | −47.0% | ~−35% |
| Money-EV $5K→ (26yr) | $5.6K | **$4.5K** | ~$18–22K |

**Verdict:** the composition does NOT beat the base (Sharpe 0.020 < 0.119 — more defensive: lower vol, shallower MDD, overlay de-grosses to cash in EVERY crisis [dotcom 0.05× / GFC 0.00× / COVID 0.00× / 2022 0.31×, T-221 checklist PASSED], but sacrifices too much return); and **neither arm beats — or even approaches — the robo** (both ci_low NEGATIVE, CAGR ~0). **Money stays in the Schwab robo — the correct decision GIVEN A FAILURE, not a success.** N_trials += 1.

**Two numbers, neither is "the base":**
- **0.751** = survivorship-INFLATED **static-109** (the table above) — not the honest base.
- **0.119** = honest PIT universe + realistic cost, but **LEVERED to 3.48× / 23% of bars** (the −63.5% MDD is a calm-2012/2004 *levered* bleed, NOT a crisis loss). Leverage source = **Engine-B per-name sizing** (`risk_engine.py:1077-1083`, no cross-name cash budget; NOT MVO weights, which sum ≤1) → fix is **B/T-232** (building). Un-deployable as-is (a $5-15K cash Roth has no margin/borrow/short).
- **Honest DEPLOYABLE base ≈ 0.1–0.3** (Sharpe is ~leverage-invariant, so 0.119 is representative; the precise un-levered MDD is UNCOMPUTED — the H0 conclusion does not depend on it, so no re-run).

**Reconciliation of the 6× gap (0.751→0.119):** dominated by **SURVIVORSHIP** (static-109 → PIT-691; risk/alpha/regime config md5s are byte-identical, so same book — only the substrate differs), then **realistic costs** (OFF→ON; the re-anchor pre-dates T-210), then **leverage** (re-anchor 2.03× vs T-215 3.48× — a MDD effect; leverage can't explain the *return* gap since T-215 levers MORE yet earns LESS). **Leverage is NOT the dominant explanation — survivorship is.** This SUPERSEDES the T-219 analytical "honest base ~0.60" estimate (it under-counted the survivorship gap; the direct PIT measurement is far lower). Detail: `docs/Audit/phase1_cloud_run_t215_2026_06_19.md`.

**Apparatus hardening shipped (T-215, post-verdict merge):** the `cloud_entrypoint.sh` pipefail-defeats-upload bug (census-failed cells uploaded nothing — cost a 30h re-run), the `discover_cached_tickers` ≥N-in-window fix (the SRCL/RX panel-shrink root cause), and a **mandatory both-paths cloud-smoke gate** (`scripts/cloud_pipeline_smoke.py`) so this is the last campaign run without a self-checked pipeline.

**🎯 HEADLINE: the 26yr is NOT a collapse — but it's BORDERLINE, not validated.** 0.751 vs the old 0.237 = **3.2× higher**; the "collapse" was the 4-way substrate artifact, not real. BUT ci_low **0.382 sits just under the 0.40 CI-aware kill line** → not killed, not validated. **After-tax taxable = 0.093 → Roth-first re-confirmed** (taxable can't run this book). The 16yr (1.162, ci_low 0.676) clears the easy crisis-light window; the **16yr→26yr gap (1.162→0.751) is the "bull-machine" signature** — strong post-2010, borderline across the full cycle (dotcom+GFC). Direct evidence for the decompose / conditional-sleeve agenda — NOT an all-weather claim. *(Saga trail: ledger T-128-CO→T-167 + MEMORY; the old 0.237/T-092 collapse + the T-155 anchors are SUPERSEDED.)*

**✅ ANCHORS CAVEAT RESOLVED (T-180-v2, 2026-06-17): the re-run is done; the base is CONFIRMED borderline — not higher, not a collapse.** T-180 baked + manifest-pinned the simfin panel, fixed the loader (cached reads need no API key) + the mtime-race, and re-ran **CENSUS-GATED** (`fundamentals_blind=0`, N=5) — the true 21-edge anchors are in the table above. The 4 value/accruals edges turned out **NEUTRAL-TO-NEGATIVE**: the 26yr is UNCHANGED at 0.751/ci_low 0.371, and the bull windows slightly DROPPED (2022 1.512→0.986; 16yr 1.162→1.105, ci_low 0.676→**0.625** — now just under the ~0.66 DSR bar too). So **the "true base is higher" hope is dead, and NOTHING strictly clears** — the base is a confirmed borderline, bull-conditional, no-validated-edge book. (T-168 also closed: the bitwise "split" was a ~5e-9 logged-metadata FP artifact, not trade nondeterminism — metrics are N=5 deterministic.) **The forward alpha lever is the un-blocked Discovery `--discover` (D's Phase-0b, on image `sha-fc5a69e`), NOT the existing edge set.** Detail: `docs/Audit/measurement_integrity_audit_2026_06_16.md` + B's T-180 audit.

**STANDING RULE (kept): no cloud verdict clears a gate until its arm0 anchor reproduces at N≥5-per-window unanimity (the cov-pin now makes this bitwise by construction); arm cells must be reproducible. Every campaign carries the arm0 anchor cell as a hard gate** — it's what caught the T-155 premature stamp.

**✅ THE FORK IS RESOLVED (USER, 2026-06-15): capability-first.** Real capital lives in a Schwab robo (it captures beta safely AND is the benchmark); NO real money in the system until it is PAPER-VALIDATED **and** demonstrably beating the robo net-of-costs/after-tax. Path A (deploy-small-live) is OFF. Now in **capability-build + edge-hunt, paper-only** mode. The two in-house crisis-defense levers are dead (de-gross T-118r, in-house sleeve T-128r), BUT the **BOUGHT managed-futures separate-account sleeve (T-170) is the FIRST lever to clear its pre-registered gates** — recommend-to-paper @ 20% DBMF (deep-crisis defense literature-pending). The paper machine is LIVE (E, T-169).

**SAGA TRAIL (all RESOLVED — detail in ledger T-125→T-167 + MEMORY; do not quote as current):** the multi-week cloud-determinism saga is closed. Sequence of root causes, each found by a controlled diagnostic and superseding the last: bytecode (T-127) → "data drift" (T-126, wrong) → the placement lottery (T-128/T-140, real: cov→MVO FP nondeterminism) → hermetic/earnings contamination (T-142/T-155) → and finally the four degradations the clean re-anchor exposed (regime-blind macro, truncated SPY/regime, `_normalize_df` 90/109-ticker load-truncation, the mean_variance os-crash — all T-167) + the lottery KILLED by the deterministic cov (T-140-fu3). **Allocator-identity RESOLVED: mean_variance is production** (the Apr-23 adaptive artifact was a stale gitignored silent override — archived T-167; per T-162 it didn't outperform). **The T-155 anchors and the T-092 0.237 collapse are SUPERSEDED** by the trustworthy anchors at top.

**If this date is more than 3 days old, read `forward_plan.md` (2026-06-06 block = current plan), `health_check.md`, and the latest `docs/Audit/*` before quoting state.** Hard caps below (≤5 per section, exactly 1 next-decision) are the anti-rot discipline — when a slot fills, the oldest item moves to MEMORY or is superseded.

---

## Validated (max 5)

- **The TRUE 21-edge anchors (T-180-v2, 2026-06-17, census-canonical `fundamentals_blind=0`, N=5).** 16yr (2010-25) Sharpe **1.105 / ci_low 0.625** — now just UNDER the ~0.66 CI-aware DSR bar (the value/accruals edges dropped it from the simfin-blind 1.162/0.676); still the crisis-light, survivor-biased UPPER-bound window = the **bull-machine, NOT all-weather.** The honest full-cycle test, 26yr **0.751 / ci_low 0.371** (UNCHANGED by the edges), is **NOT validated** (ci_low under the 0.40 line; SR 0.751 ≪ the ~1.55 MBL bar at honest-N) but **NOT a collapse**. After-tax taxable → **Roth-only-viable.** **Net: NOTHING strictly clears, the "true base is higher" hope is REFUTED, and there is no validated edge in the existing book** — the forward alpha lever is the un-blocked Discovery `--discover` (Phase-0b, image `sha-fc5a69e`), not the current edge set.

## Recently refuted / superseded (max 5, rolling)

- **T-092 deep-substrate baseline (FINAL, n=5/16-yr, n=4/26-yr)** — refutes "a deeper window will validate the base." Sharpe INVERTS with depth: 5yr 0.60 → 12yr 0.81 → **16yr 1.018 (ci_low 0.560, CAGR 11.0%, MDD -15.4%)** → **26yr 0.246 (ci_low -0.119, CAGR 2.64%, MDD -59.3%)**. 16-yr clears MBL + point-DSR + ci_low>0 (strongest cell ever); 26-yr FAILS every gate (8/26 years negative, worst 2008 -1.28, underperforms buy-hold SPY ~4%/yr). **Strict `ci_low > DSR-benchmark` (0.66) FAILS on EVERY window** (12yr 0.33, 16yr 0.56) — by CLAUDE.md `[NN-SHARPE-CI]` nothing is formally validated. 16-yr is the crisis-free window (excludes 2008+dotcom); base is bull-conditional. **⚠️ 16-yr leg provenance-UNVERIFIED post-T-127** (pyc-taint class; pinned re-reads ranged 0.945-1.021) — C's running campaign captures the clean 16-yr arm0 anchor; do not quote 1.018/ci_low-0.560 as precise until then (the 26-yr leg IS clean-verified: 0.237). Survivor-only → UPPER bounds. **PIVOT SIGNAL.** Also: determinism drift scales with window depth (26yr 0.19, >3-Sharpe per-year swings) — T-057c-det-followup insufficient at long windows. Branch `feature/deep-substrate-baseline-t092` @ 15bdd97. Audit: `deep_substrate_baseline_t092_2026_05_31.md`. **⚠️ SUPERSEDED 2026-06-14 (T-167 re-anchor): the 0.237/0.246 "26yr collapse" was a 4-WAY SUBSTRATE ARTIFACT — NOT real.** Clean trustworthy 26yr = **0.751 (ci_low 0.382)**, 3.2× higher (the old runs were degraded by the lottery + regime-blind macro + truncated SPY/regime + a `_normalize_df` clip that ran the "26yr" on a ~19-ticker universe). **The "collapse is real / pivot premise holds" conclusion is WRONG.** The bull-conditionality survives (16yr 1.162 > 26yr 0.751) but the gap is FAR smaller than the artifact suggested (was 1.018 vs 0.246). The base is borderline-real, not collapsed — see Validated above. *(The T-126 "0.246 re-verified" was reproducing the SAME degraded substrate 3×; reproducibility ≠ correctness.)*
- **T-098 H-Band no-trade bands** — REFUTED at ±20%/±25% on 12-yr (Δ Sharpe +0.008/+0.018, ci_low NEGATIVE both; turnover Δ <2% vs predicted −60-70%; skew mixed, arm2 worse). Band suppresses small rebalances but not the dominant large vol-target ones (Donohue-Yip calibrated to monthly concentrated books, not our daily inverse-vol 30-name). Clean Engine C impl (default-OFF, canon-OFF=baseline, ON-differs, det 3/3) kept additive for a future tighter-band sweep. Branch `feature/h-band-no-trade-t098` @ bf9da13 NOT merged (refuted+inert). Audit: `no_trade_band_h_band_t098_audit_2026_05_31.md`. **B's 12-yr arm0_off Sharpe 1.314 is MEAN-OF-12-ANNUAL, NOT comparable to pooled-full-window 0.81 — director-verified from cell JSON; 0.81 STANDS (same statistic-mismatch as the old Foundation-Gate 1.296 → pooled 0.507).**
- **T-095 fill-convention (H-Convention)** — REFUTED (good outcome): backtest already fills at **t+1 OPEN**, not close-to-close. Lou-Polk-Skouras overnight-alpha-leak does NOT apply; ~0.81 was never fill-inflated. Director-verified `execution_simulator._next_price_for_entry_exit`. Audit: `fill_convention_diagnostic_t095_2026_05_31.md`.
- **T-057 confidence-gated execution (N≥3)** — REFUTED on 12-yr (Δ Sharpe -0.128; p(Δ>0)=32%). 5-yr-Alpaca +0.793 was a substrate-conditional artifact. Do NOT flip. Audit: `multi_year_window_harness_t053b_2026_05_25.md`, `confidence_gated_flag_flip_t057b_2026_05_24.md`.
- **T-055e/g/h regime-conditional vol-target** — CLOSED on 12-yr (Δ Sharpe -0.214). 5-yr-Alpaca +0.549 "DEFENSIBLE" retired; T-055 arc monotone decay (+0.549 → +0.413 → -0.214) as rigor rose. Audit: `vol_target_12yr_verify_t055h_2026_05_29.md`.

<!-- Dropped to honor ≤5 cap (both fully captured in MEMORY.md): T-088 risk_per_trade_pct dead-knob (project_t088_risk_per_trade_dead_knob); T-087/T-089 Engine E reversal — HMM hmm_p_crisis IS predictive (AUC 0.887, 12-yr causal), the validated signal the Path-B kill-switch would use (project_engine_e_reversal_predictive). -->

> **Load-bearing for the decision above:** Engine E's `hmm_p_crisis` is VALIDATED-predictive (T-087/T-089, AUC 0.887) — it's the signal a Path-B HMM kill-switch would gate on. Full entry in MEMORY.

## In flight (max 5)

_**THE PIVOT (user 2026-06-18): goal RESOLVED → re-architecture executing.** Both external reviews (code audit + domain research) converged: stock-selection alpha at retail is dead AND we'd been too narrow (we defined alpha too strictly, tested 109 survivors, never combined fundamental+technical conditionally, never used the winnable game). New goal = "an autonomous system that DOES WELL = beats the Schwab robo net-of-cost/after-tax on risk-adjusted/tail terms" — NOT academic alpha. Plan: `docs/State/re_architecture_plan_2026_06_18.md`. Rigor kept (the target changes, the honesty doesn't). 5 fronts now in flight:_

**✅ PHASE 0 COMPLETE + the Phase-1 COMPONENTS are built — at the CONVERGENCE POINT.** All merged today:
- **C/T-203 (KEYSTONE):** the robo scorecard IS the deploy gate now (Roth-primary, after-tax, ci_low vs robo OR ≥20% MDD), correctly returns **DO NOT DEPLOY** on the base (honest "not yet") + is SMART — **discounts a bull-window-flattered MDD, requires the tail be CRISIS-TESTED** (dotcom/GFC/COVID). Gate-6 OLS→HAC fixed. Canon 80b501a8 bitwise. Factor-orthogonality demoted to diagnostic.
- **The components (OFF-default, ready to compose):** trend overlay (E/T-204 — **MDD ~halved**, the standout win), defensive screens (A/T-205), the realistic-cost mode (D/T-210), the `is_it_beta_or_edge()` factor diagnostic (B/T-209), the vol-target spec (B/T-206, **Engine-B flip = propose-first**).
- **The honest base picture — ⛔ T-219's analytical ~0.60 SUPERSEDED by T-215's DIRECT measurement (≈0.1–0.3):** T-219 (analytical, PIT-cap re-rate) estimated the honest base at ~BORDERLINE (~0.60) by applying only a ~−0.10 cost drag + ~−0.045 survivorship to the 0.751 — but that **under-counted the survivorship gap** because 0.751 was static-109, not the honest PIT base. **T-215's first DIRECT measurement on the PIT × realistic-cost substrate (26yr, census-canonical) = Sharpe 0.119 (levered) → honest deployable ≈ 0.1–0.3**, far below the ~0.60 estimate. So the honest base does NOT clear the 0.40 line, ci_low is negative, and it does not beat the robo — see the T-215 block at the top. *(T-219's mechanism findings still hold — closet-beta α t_HAC=1.31, delisted cohort mostly mid-cap; only the analytical base-Sharpe estimate is superseded by the direct number.)*

**→ NEXT (the convergence): the Phase-1 COMPOSITION + the FIRST HONEST beat-the-robo measurement.** Compose trend + defensive + vol-target (+ realistic costs, + lower-turnover) on the PIT universe, **crisis-tested**, vs the robo gate, with the factor diagnostic checking beta-vs-edge. This is a **propose-first integration** (wires OFF-default components into sizing + the Engine-B vol-target flip) + a **cloud campaign** (compute-bound). See the next-decision.

**Also:** E now free; **6/22 CLS landing + held-verify + ENABLE** is the director's (on the fixed rev5 image). Phase-0 archive verified + HELD until Phase 1 settles. The realized PIT re-baseline is a cloud cell.

## Next decision (exactly 1)

- **🎯 THE SWING QUESTION IS ANSWERED — H0 (T-196): the single-gene Foundry vocabulary has NO alpha in the existing book. The cheap, in-the-existing-feature-space alpha hunt is now EXHAUSTED. STRATEGIC FORK — user's call.**
  - **What's now closed (the honest pile of nulls):** base borderline + no validated edge (T-180-v2); VRP refuted (T-174); cross-sectional 0/16; vol-target + confidence-gate refuted (T-055h/T-053b); crisis-timing dead (T-118r/T-128r/T-178; always-on 20% bought-MF is the ceiling, safe_f 0.928); **and now single-gene Discovery = H0 (T-196, 0/35).** After-tax the base LOSES to the robo (T-191, taxable → Roth-only). **There is no found alpha and no cheap lever left to find it in the current vocabulary.**
  - **The three forward levers, honestly weighted:**
    - **(1) Extend the vocabulary** — multi-gene composites / untested feature classes (B's note). Real but **LOW prior**: single-gene found exactly nothing and the existing book has no validated edge, so combinations of individually-useless features rarely rescue. More cloud R&D, uncertain payoff.
    - **(2) New modality — the LLM-as-analyst checkpoint** (A's T-184/T-190). Its "Discovery comes up empty" gate is **NOW MET.** Genuinely *different* (narrative/thematic, plausibly orthogonal, positive-skew-aligned) — but T-190's own verdict is it **CANNOT be cleanly backtest-validated at retail-N → validate-FORWARD-only (paper).** High-effort, high-uncertainty, slow to prove.
    - **(3) Consolidate + measure honestly** — accept the base (borderline, Roth-only) + the bought-MF crisis floor as the capability, finish validating the PAPER MACHINE (E), and measure base-vs-robo net-of-cost/after-tax on the scorecard. Per the fork resolution, **"money stays in the robo" is an acceptable honest outcome** — and the accumulated evidence now points that way.
  - **⚠️ REFRAMED by the drawing-board research (`capability_gap_drawing_board_2026_06_18.md`, user-directed):** the user pushed back ("we've been too narrow — conditional edges, positioning, market structure") and the 6-lens map proved it. **(a) H0 was NARROW** — it only tested the GA's UNIVERSAL single-gene price-feature vocabulary; the dormant macro/positioning/structural library + all CONDITIONAL forms were never in that test. **(b) Worse truth:** the 0.751 base is probably NOT edge-alpha — all 13 dense edges are closet-beta (negative factor-α, T-117) and the 3 flow edges carrying ~94% of PnL (volume_anomaly/gap_fill/herding) are factor-negative → the Sharpe is **beta + risk-mgmt + survivorship**, not stacked edge alpha. **(c) The one genuine open door = REGIME-CONDITIONAL reconstruction:** the base is provably bull-conditional, the HMM crisis posterior is validated (AUC 0.887), `signal_processor.py` regime_gate plumbing EXISTS but is fed empty dicts (`regime_conditional_enabled=false` since Apr); it was tried net-negative ONCE — but on the COARSE 5-axis advisory regime, NOT the validated HMM (input-quality failure, not mechanism). **This is the user's own "decompose, don't require all-weather" directive, never actually executed.** **KILLED honestly:** dealer-gamma/options-flow/prime-broker (institutional-only data, NOT retail), cross-asset sleeves (Alpaca equity-only), the refuted edges. **Prioritized plan (repoint-first):** (1) edge alpha attribution on 26yr — cheap truth, is it alpha or beta? (2) paper machine + base-vs-robo scorecard = the deploy GATE; (3) regime-conditional book reconstruction w/ the HMM = the one real research push; (4) positioning-data falsification (cheap, modest prior, user-requested); (5) multi-gene/regime-stratified Discovery (low prior); (6) LLM-analyst (last, forward-only). **NOT yet at "concede" — doors #1/#3 are real + unopened. AWAITING user's direction on the plan.**
- _(context) Crisis-defense CLOSED (T-178; always-on 20% bought-MF ceiling, safe_f 0.928). Measurement-integrity arc COMPLETE (census T-181 + loader-HALT T-194 + the last swallow T-199; 2 CLAUDE.md non-negotiables codified). The base anchors stand (T-180-v2, T-199-confirmed-clean). Plan of record: `forward_plan.md` 2026-06-15 + GOAL.md — UPDATE pending the user's fork call._

## Standing constraints (max 5)

- **Honest N_trials:** `run_registry` shows **125 rows**; effective ~**260+** including cloud cells not all back-synced into the registry. MBL bar rises with N — every new measurement on the same substrate raises the threshold the next one must clear.
- **MBL at SR ≈ 0.81 base:** ~**17 years required** (Bailey-Borwein-López de Prado-Zhu 2014). 12-yr window had ~12 → borderline. 26-yr (T-092 in flight) is the test. The 5-yr substrate-honest window would require SR ≥ 1.55 to clear DSR — our base cannot.
- **5-yr-window measurements are statistically under-powered at current N** — they are exploratory only, NEVER flag-flip evidence. CLAUDE.md non-negotiable `[NN-MBL]` codifies this.
- **Canonical substrate:** extended Stooq + Alpaca dividend-strip merged (T-082b). Survivors to 1962/1970; delisted missing pre-2020 (survivorship caveat). Any positive lift measured on a different substrate (e.g., Alpaca-only) MUST be re-verified on the canonical before flag-flip recommendation (CLAUDE.md `[NN-SUBSTRATE-REVERIFY]`).
- **Corrected baseline Sharpe ~0.81 (12-yr), ci_low ~0.33** — plausibly-real, NOT formally validated. Bootstrap CI required on every Sharpe headline (CLAUDE.md `[NN-SHARPE-CI]`); kill-thresholds compare against `ci_low`, not point-estimate.

---

*Edited at session end. Hard caps per section. When content falls off the rolling cap, move to `MEMORY.md` or supersede with a newer entry. `forward_plan.md` carries the verbose, narrative version of in-flight planning; `CURRENT_STATE.md` is the at-a-glance dashboard.*
