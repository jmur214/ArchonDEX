# CURRENT_STATE — ArchonDEX

**Last reconciled with source docs:** 2026-06-13 (T-128-CO merged — **P0: THE PLACEMENT LOTTERY IS LIVE; the published anchors are NOT durable**)

**🔴 P0 — THE LOTTERY SURVIVES T-140's THREAD PINS; the T-155 anchors do NOT reproduce on their own certified image (T-128-CO, 2026-06-13).** A's sleeve relaunch on `sha-5323a3c`/`t155-anchor:3` drew arm0 16-yr `9153ff15`/**0.945** (the MINORITY attractor), NOT the published `62db5c0d`/1.021 (anchor gate FAIL); the 2022 canary split **2-vs-1 within one pinned batch** (`0a62b754`×2 vs `0c6b8811`×1 — nondeterminism on the certified image, full stop); arm0 26-yr did reproduce (`529e5520`/0.237). Cross-check vs original T-128: **3/6 cells reproduce bitwise across two images, 3 flip → pure per-task lottery, image-independent.** Mechanism bounded by 18 probes (fleet 8/8 uniform Graviton2, eigh 8/8 bitwise, SLSQP 6/6 bitwise, MVO universe sorted — all isolated culprits RULED OUT): residue = FP-dust in the full-pipeline **cov()→MVO composition** on zero-weight names (BKNG 3.73e-17 vs 0.0) tipping a rounding threshold → flips a trade. **T-140 pins fixed the KERNELS (proven); a COMPOSITION-level source survives.** Note the connection: the cov()→MVO path is the *mean_variance* path the CLOUD exercises (T-158) — the same path T-118's de-gross arms run on. **DIRECTOR CORRECTION (owned): my T-155 "gate cleared / lottery RESOLVED / canon continuity" stamp was PREMATURE — a 9/9 unanimous batch at N=3/window cannot distinguish p=0 from p-small; the standing N≥5-per-window rule was not met. The launcher's hard anchor-gate (required of every campaign) is what caught this — guardrail worked, stamp was wrong.**

**STANDING RULE (reaffirmed): no cloud verdict clears a gate until its arm0 anchor reproduces with N≥5-per-window unanimity, AND arm cells are reproducible (single draws are exploratory only).** Every campaign carries the arm0 anchor cell as a hard gate.

**WHERE THIS LEAVES THE RELAUNCHES:** A's sleeve A/B = INVALID at 16-yr / ungated at 26-yr (chapter OPEN). **C's T-118 de-gross campaign = lottery-exposed (16-yr arm0 anchor is the unstable 1.021; arm0/arm_on are independent task draws → the Sharpe-DIFFERENCE gate inherits both draws' lottery noise) → VERDICT HELD.** Critical path: **B's determinism bisect of the cov()→MVO composition (T-140-followup-2)**, then re-run the gated reads with the fix (single draws OK again) or with N≥5/cell (if irreducible). Fork measurement base is otherwise complete (T-154 survivor inflation; T-162 allocator H-mix); **the fork still convenes on the T-118 + sleeve verdicts — but those verdicts are now downstream of the determinism fix.**

**⚠️ ALLOCATOR DIVERGENCE (T-158, 2026-06-12): local and cloud have run DIFFERENT TRADING SYSTEMS since 2026-04-23.** A learned artifact (`data/research/allocation_recommendations.json`, fail-quiet loader, `mode` in policy.py:138 safe-keys) flips every LOCAL allocate() to **adaptive** (vol-target + exposure-cap overlays LIVE, max_weight 0.15, target_vol 0.10); the file isn't in the cloud image, so every CLOUD canonical number (529e5520/0.237, 62db5c0d/1.021, the T-092 collapse, all campaigns) ran **mean_variance** (overlays dead, max_weight 0.30). All LOCAL canons (0145c03a family) = the adaptive system. **Cross-substrate comparisons are allocator-confounded; never quote local-vs-cloud deltas as same-system.** PRE-REGISTERED DISCRIMINATOR (logged BEFORE B's anchors land): T-155's bonus test (cloud 2022 anchor vs local 0145c03a) now separates two hypotheses — earnings-pin-only predicts MATCH; allocator-divergence predicts MISMATCH (both mechanisms can coexist; allocator still differs post-pin, so expectation = MISMATCH; a match would mean the allocator delta doesn't bite that cell's trades and needs its own follow-up). **Allocator-identity decision (archive the Apr-23 artifact vs commit its intent to config) is DIRECTOR-HELD, deliberately POST-anchors** — touching it now re-baselines local canons mid-flight (same trap class as the factors= wiring). It is also a PRE-PAPER-TRADING gate: paper would run the local (adaptive) machine while all deep-window evidence is about the cloud (mean_variance) machine.

**⚠️ SUPERSEDED BY T-128-CO (2026-06-13) — "LOTTERY DEAD" WAS WRONG; see the P0 banner at top. The T-140 N=5 batches were lucky draws, not proof; the lottery survives the thread pins (composition-level cov/MVO source). Retained below for the saga trail only.** ~~**✅ LOTTERY DEAD (T-140, 2026-06-11): 26yr `529e5520`/0.237 ×5 bitwise + 16yr `62db5c0d`/1.021 ×5 — collapse confirmed WITHOUT caveats (1.021 vs 0.237).**~~ ⚠️ ONE LAST GATE (T-142): earnings_vol_edge fetched LIVE Yahoo dates into trades all along — A's anchors are pre-hermetic/date-fragile; T-155 (earnings-dates manifest pin + hermetic rebuild + re-anchor) gates the relaunches.** Then: C's overlay campaign + sleeve A/B + the pre-registration batch (buffering, vol-estimator) + THE FORK. Historical note (T-128 era): A's 18-probe forensics: two canon attractors (2b2f2c2b/0.446 vs 529e5520/0.237) flip WITHIN one image digest — multithreaded LAPACK eigh task-instability (fixed 6/6 by OMP/OPENBLAS=1) + task-unique listdir order. T-125/126/127 attributions were confounded single draws (pyc hygiene stays; causal story doesn't). **NEITHER 0.237 NOR 0.446 is canonical — T-140's N≥5 unanimity re-baseline decides.** Local/within-task results unaffected; cross-task A/Bs since Jun-6 carry ±0.21 lottery. C's campaign halted pre-grid. Prior banner (historical): the 0.446 was **stale host BYTECODE** (rooted `__pycache__/` in .dockerignore let nested Mac-host pycs into every image; container Python loaded 237 of them in place of source — py3.14.4 vs .5). T-126's "data-drift" attribution was wrong: **zero data ever drifted** (14,037 files byte-identical; T-082 intact, now manifest-pinned `a40d5483` + S3-snapshotted). **Canonical clean 26-yr arm0 (current prod config, hmm-ON): Sharpe 0.237 / CAGR 2.51% / MDD -59.29%, canon `529e5520` — bitwise-reproduced by 3 independent builds across 3 days.** 0.446 RETIRED; 0.246 = hmm-OFF-era config. **T-092's collapse STANDS; the mid-saga "HMM rescues depth" hope was the artifact.** Build pipeline: `scripts/build_backtest_image.sh` ONLY (git-archive clean-source + manifest verify); June-6-era cloud absolutes are provenance-unverified (within-image A/B verdicts directionally survive). Methodology: single-cell OFF-canon ≠ 26yr-inert (~0.009 benign leak, T-126).

**If this date is more than 3 days old, read `forward_plan.md` (2026-06-06 block = current plan), `health_check.md`, and the latest `docs/Audit/*` before quoting state.** Hard caps below (≤5 per section, exactly 1 next-decision) are the anti-rot discipline — when a slot fills, the oldest item moves to MEMORY or is superseded.

---

## Validated (max 5)

- [none yet — no substrate-robust, factor-adjusted, MBL-clearing positive finding as of 2026-05-31. Corrected baseline Sharpe ~0.81 on 12-yr is **plausibly-real but not formally validated** (`ci_low ~0.33` doesn't clear DSR; window doesn't clear MBL at honest N). See `docs/Audit/baseline_dsr_mbl_foundational_2026_05_30.md`.]

## Recently refuted / superseded (max 5, rolling)

- **T-092 deep-substrate baseline (FINAL, n=5/16-yr, n=4/26-yr)** — refutes "a deeper window will validate the base." Sharpe INVERTS with depth: 5yr 0.60 → 12yr 0.81 → **16yr 1.018 (ci_low 0.560, CAGR 11.0%, MDD -15.4%)** → **26yr 0.246 (ci_low -0.119, CAGR 2.64%, MDD -59.3%)**. 16-yr clears MBL + point-DSR + ci_low>0 (strongest cell ever); 26-yr FAILS every gate (8/26 years negative, worst 2008 -1.28, underperforms buy-hold SPY ~4%/yr). **Strict `ci_low > DSR-benchmark` (0.66) FAILS on EVERY window** (12yr 0.33, 16yr 0.56) — by CLAUDE.md #6 nothing is formally validated. 16-yr is the crisis-free window (excludes 2008+dotcom); base is bull-conditional. **⚠️ 16-yr leg provenance-UNVERIFIED post-T-127** (pyc-taint class; pinned re-reads ranged 0.945-1.021) — C's running campaign captures the clean 16-yr arm0 anchor; do not quote 1.018/ci_low-0.560 as precise until then (the 26-yr leg IS clean-verified: 0.237). Survivor-only → UPPER bounds. **PIVOT SIGNAL.** Also: determinism drift scales with window depth (26yr 0.19, >3-Sharpe per-year swings) — T-057c-det-followup insufficient at long windows. Branch `feature/deep-substrate-baseline-t092` @ 15bdd97. Audit: `deep_substrate_baseline_t092_2026_05_31.md`. **✓ RE-VERIFIED — 0.246 STANDS (T-126, 2026-06-09):** the T-125 0.446 was a DATA-DRIFT artifact (the Dockerfile bakes data/processed+data/raw from the host, UNPINNED → drifts across builds). A fresh pinned build reproduces arm0 26yr 0.246 to 3 decimals. **The collapse is real; the pivot premise holds.** New infra critical path: pin the data substrate (T-127).
- **T-098 H-Band no-trade bands** — REFUTED at ±20%/±25% on 12-yr (Δ Sharpe +0.008/+0.018, ci_low NEGATIVE both; turnover Δ <2% vs predicted −60-70%; skew mixed, arm2 worse). Band suppresses small rebalances but not the dominant large vol-target ones (Donohue-Yip calibrated to monthly concentrated books, not our daily inverse-vol 30-name). Clean Engine C impl (default-OFF, canon-OFF=baseline, ON-differs, det 3/3) kept additive for a future tighter-band sweep. Branch `feature/h-band-no-trade-t098` @ bf9da13 NOT merged (refuted+inert). Audit: `no_trade_band_h_band_t098_audit_2026_05_31.md`. **B's 12-yr arm0_off Sharpe 1.314 is MEAN-OF-12-ANNUAL, NOT comparable to pooled-full-window 0.81 — director-verified from cell JSON; 0.81 STANDS (same statistic-mismatch as the old Foundation-Gate 1.296 → pooled 0.507).**
- **T-095 fill-convention (H-Convention)** — REFUTED (good outcome): backtest already fills at **t+1 OPEN**, not close-to-close. Lou-Polk-Skouras overnight-alpha-leak does NOT apply; ~0.81 was never fill-inflated. Director-verified `execution_simulator._next_price_for_entry_exit`. Audit: `fill_convention_diagnostic_t095_2026_05_31.md`.
- **T-057 confidence-gated execution (N≥3)** — REFUTED on 12-yr (Δ Sharpe -0.128; p(Δ>0)=32%). 5-yr-Alpaca +0.793 was a substrate-conditional artifact. Do NOT flip. Audit: `multi_year_window_harness_t053b_2026_05_25.md`, `confidence_gated_flag_flip_t057b_2026_05_24.md`.
- **T-055e/g/h regime-conditional vol-target** — CLOSED on 12-yr (Δ Sharpe -0.214). 5-yr-Alpaca +0.549 "DEFENSIBLE" retired; T-055 arc monotone decay (+0.549 → +0.413 → -0.214) as rigor rose. Audit: `vol_target_12yr_verify_t055h_2026_05_29.md`.

<!-- Dropped to honor ≤5 cap (both fully captured in MEMORY.md): T-088 risk_per_trade_pct dead-knob (project_t088_risk_per_trade_dead_knob); T-087/T-089 Engine E reversal — HMM hmm_p_crisis IS predictive (AUC 0.887, 12-yr causal), the validated signal the Path-B kill-switch would use (project_engine_e_reversal_predictive). -->

> **Load-bearing for the decision above:** Engine E's `hmm_p_crisis` is VALIDATED-predictive (T-087/T-089, AUC 0.887) — it's the signal a Path-B HMM kill-switch would gate on. Full entry in MEMORY.

## In flight (max 5)

- **T-140-followup-2 (Agent B — THE P0 CRITICAL PATH, dispatched 2026-06-13): bisect the cov()→MVO composition** to name + pin the lottery's surviving FP source (capture the Σ-matrix bytes per task inside a real run; A's 18 probes ruled out every isolated kernel — eigh/SLSQP/universe-order). Either pin it (single draws deterministic again) or prove it irreducible (N≥5/cell becomes the permanent measurement protocol). **Everything below waits on this.**
- **T-118 campaign (Agent C) — VERDICT HELD (lottery-exposed).** Cells launched/landed on the anchors but the campaign runs 16-yr (arm0 anchor == the unstable 1.021) and 26-yr; arm0/arm_on are independent task draws so the Sharpe-DIFFERENCE gate inherits both draws' lottery noise. C dispatched to: report per-window in-campaign arm0 anchor checks FIRST, read the current batch as EXPLORATORY/directional only, re-run the gated read after B's fix (or with N≥5/cell). **T-162 DONE+merged** (allocator H-mix; artifact ~1.1 Sharpe).
- **Sleeve A/B (Agent A) — DONE as a P0 (T-128-CO merged):** the relaunch caught the anchors failing; A/B INVALID at 16-yr, ungated at 26-yr, sleeve chapter OPEN. Harness staged to re-run once the substrate delivers N≥5/window arm0 unanimity (i.e., after B). **A: free** pending B.
- **T-154 DONE (merged): per-strategy survivor inflation = ΔCAGR −3.54pp / ΔSharpe −0.085 (lower bound); T-144 absorption HELD** — absolute CAGR narratives inflated, risk-adjusted verdicts survive. Queued small fixes: ensure_data no-timeout fetch (harness footgun); PIT-mask fail-open read.
- **PAPER TRADING APPROVED (user, 2026-06-12): all three T-159 decisions** — design adopted (incl. stub-archival ratification), PR-1/2 build authorized, config ratified (Roth-emulation $5K, dyn-opt ON, §5 promotion criteria; revisable pre-go-live only). **T-160 (Agent E, in flight): PR-1 OrderManager/journal/ledger + PR-2 ReconciliationEngine/dry-run scheduler — pure-new `paper_trader/` only, zero existing files.** PR-3 stays propose-first; PR-4 hard-gated. Pre-go-live gates unchanged: allocator-identity decision (banner) + book choice (fork).
- **T-161 (Agent D, in flight): the harness-fix bundle D root-caused** — ensure_data bounded timeout (fail-loud), PIT-mask fail-loud + `pit_mask_fallback_bars`, definitive OFF-proof close-out. Canon-md5 + det ×2 receipts required.
- **A, C: launch-ready hold on B's anchors** (C: T-118 one-command; A: sleeve A/B relaunch). Queued: T-113 drawdown-lift A/B (post-overlay); LPS shelf cell re-reads iff paper auction telemetry < 1bp/side (T-157 contingency).
- **ALPHA LANE RE-OPENED on the untested categories (2026-06-10 director correction — the "substrate-empty EVIDENCED" claim was OVER-SCOPED).** What T-117→122→123→129 actually closed: {our 13 artisanal edges, equity-proxy VRP, BAB-class low-beta} on {daily bars, S&P-survivor large/mid universe, the cross-sectional harness}. That is the **characteristic-factor + artisanal-technical lane** — closed, solidly. NOT yet tested (and several can clear an FF5-gate where characteristic factors can't *by construction*): canonical event/data edges (8-K classes, Form-4 insider clusters at depth, 13F crowding/Antón-Polk, earnings-guidance text), intraday-derived FEATURES for daily signals + the one survivable intraday strategy (GHLZ first-half-hour), cross-asset CARRY + rotation (only TREND was tested), VIX term-structure alpha, non-linear edge interaction (metalearner — built, never trained; the MI/H-stat go/no-go diagnostic was never run), meta-labeling, DHS canonical FIN. Fork options = (a)/(b)/(c)/(d=untested categories). EXECUTING: T-132 mapped 16 categories (done) → T-135 LPS closed (first strict-gate α ever, +13.9%/yr t=5.69 overnight — unharvestable; gate VALIDATED) → THE STRUCTURED-EVENT LANE CLOSED AS A CLASS (T-137 8-K + T-144 Form-4 + T-145 13F: all dual-universe/family-wise clean at depth). Next alpha thread: metalearner-as-FALSIFICATION (T-132's weak-prior GO, strict pre-registered kill bar), then intraday-derived features. Deployment lane (E) has shipped: dyn-opt, after-tax gate+router, crisis harness, auction execution, buffering in flight.
- **Methodology guard — queued.** Single-cell OFF-canon ≠ 26yr-inert (T-126, ~0.009 leak): add a deep-window canon check to future inertness claims (contract-test/doc-lint extension).

## Next decision (exactly 1)

- **THE GATE CLEARED (T-127, 2026-06-10): reproducibility pinned end-to-end (base digest + lib lock + substrate manifest + git-archive builds; 3-builds-1-canon proof). The held wave LAUNCHES — C's T-118 overlay first.** The de-gross verdict is the next decision-shaping result: does regime-transition de-gross cut the confirmed-real -59% 26-yr MDD at non-worse Sharpe (pre-registered: ci_low>0 on the Sharpe DIFFERENCE + MDD −≥25% + no single-event dependence)? Strategic state: T-092's collapse STANDS (canonical clean 26yr arm0 = **0.237 hmm-ON**, canon `529e5520`); the alpha track RE-OPENED on the frontier map (T-129 closed the fair-BAB question; T-132 mapped 16 untested categories; D is executing them — T-135 done, T-137 in flight); the architecture/mission fork convenes on C's overlay + A's sleeve verdicts. **Plan of record: `forward_plan.md` 2026-06-06 block** (CAVEAT: its alpha-track specifics — VRP-first, sleeve build-order — are superseded by the T-132 frontier map + T-121/T-129 verdicts; a plan-of-record rewrite is queued for the fork). Everything below this bullet is 2026-06-04 sequencing context, partially executed — retained for the trail, not the current plan.
- _(historical context — 2026-06-04)_ **PATH B IN EXECUTION. MASTER FINDING (T-100, 2026-06-04): the crisis defenses EXIST in code but were STARVED in the backtest — that is why the base drew -59% on 26-yr, NOT an absence of defensive machinery.** B's instrumentation: (a) HMM **not wired** — 0/1174 advisory calls received `hmm_proba` (`hmm_enabled=false` default); (b) 5-axis regime detector **MISSED COVID** (0 crisis bars May–Dec 2020); (c) regime returns None pre-2020 on the local SPY gap (~80% of a local 26-yr run dormant); (d) the Engine-B `risk_scalar` 46% cut the audit flagged lives on **DEAD Path B** (atr-risk, confirms T-088 — prod uses Path A target_weight; the static audit-read was wrong, B's empirical trace is right). **In flight / sequenced:**
  - **T-099 (MERGED 253a96f):** long-window FP-determinism fixed (5 sites; 12-yr `--runs 3` bitwise-identical). Cloud cross-container 26-yr verify still recommended (T-099-verify).
  - **Phase 0+ (T-101 DONE 2026-06-04, Agent B):** `hmm_enabled=true` flipped in `config/regime_settings.json`. Q1' YES — posterior flows (1174/1174 advisory calls). Q2'/Q3' NO change — regime_summary distribution + per-arm gross deltas + 2022 default-cell canon are BITWISE IDENTICAL pre/post-flip. **CAPABILITY failure, not WIRING.** HMM modulates `risk_scalar` on dead Path B; Path A (production target_weight sizing) doesn't consume it. The −59% MDD was never going to be saved by a config flag. Det 3/3 PASS. Flag stays flipped (no-op for trades, observability win). Audit: `hmm_wire_phase0plus_t101_2026_06_04.md`.
  - **Phase 0b (cloud cell):** 2008 + 2000-02 need cloud data (local SPY starts 2020-04) to test crisis response on the real GFC/dotcom regimes.
  - **Phase 1 (Engine B, propose-first, 2nd gate):** binary de-gross to floor=0.25 (cash) — warranted per T-100 outcome (c), but design must REPLACE/COMPOSE-WITH the existing (now-to-be-fed) advisory de-gross, not stack a 5th cut. Returns for review.
  - **Phase 2 A/B (gated on T-099✓ + Phase-0+):** arms off/0.25/0.50/0.0/tiered, 16-yr + 26-yr, bootstrap CI. Adopt iff MaxDD −≥25% AND Sharpe-ci_low not down.
  - **Parallel fast win (APPROVED 2026-06-04):** drawdown kill-switch (already built, default-OFF, warn5/degross10/halt15) — enable + A/B. Plus capability_ledger.md + contract Layer-3 doc-fix (APPROVED). Plus F regime-conditional per-edge kill re-verify+enable (APPROVED).
  - **LAYER 2 — trend sleeve:** scaffold ALREADY EXISTS (`engine_c/sleeves/`, unwired, gauntlet-failed on megacaps) — wire+re-test, don't rebuild.
  - **Path A NOT pursued.** Deprioritized: H-Tax T-097.

## Standing constraints (max 5)

- **Honest N_trials:** `run_registry` shows **125 rows**; effective ~**260+** including cloud cells not all back-synced into the registry. MBL bar rises with N — every new measurement on the same substrate raises the threshold the next one must clear.
- **MBL at SR ≈ 0.81 base:** ~**17 years required** (Bailey-Borwein-López de Prado-Zhu 2014). 12-yr window had ~12 → borderline. 26-yr (T-092 in flight) is the test. The 5-yr substrate-honest window would require SR ≥ 1.55 to clear DSR — our base cannot.
- **5-yr-window measurements are statistically under-powered at current N** — they are exploratory only, NEVER flag-flip evidence. CLAUDE.md non-negotiable #7 codifies this.
- **Canonical substrate:** extended Stooq + Alpaca dividend-strip merged (T-082b). Survivors to 1962/1970; delisted missing pre-2020 (survivorship caveat). Any positive lift measured on a different substrate (e.g., Alpaca-only) MUST be re-verified on the canonical before flag-flip recommendation (CLAUDE.md #9).
- **Corrected baseline Sharpe ~0.81 (12-yr), ci_low ~0.33** — plausibly-real, NOT formally validated. Bootstrap CI required on every Sharpe headline (CLAUDE.md #6); kill-thresholds compare against `ci_low`, not point-estimate.

---

*Edited at session end. Hard caps per section. When content falls off the rolling cap, move to `MEMORY.md` or supersede with a newer entry. `forward_plan.md` carries the verbose, narrative version of in-flight planning; `CURRENT_STATE.md` is the at-a-glance dashboard.*
