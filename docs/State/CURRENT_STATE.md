# CURRENT_STATE — ArchonDEX

**Last reconciled with source docs:** 2026-06-14 (T-167 + PR-3 merged — **cloud substrate now COMPLETE & B's re-anchor is GO; the paper order-execution loop is merged**. The old anchors were degraded FOUR ways — lottery + regime-blind-macro + truncated-SPY/regime + load-truncated-90/109-tickers (+ a recent mean_variance os-crash) — ALL now fixed. The 26yr WINDOW was always real (calendar = ticker-union, not SPY); the clean re-anchor is the first trustworthy number.)

**✅ THE SUBSTRATE SAGA IS CLOSED (2026-06-14). THE LOTTERY IS DEAD (proven), and the "26yr collapse" was a SUBSTRATE ARTIFACT.** B's deterministic cov (fixed-order einsum replacing pandas' OpenBLAS gemm) makes Sigma byte-identical across tasks → all 3 windows are **N=5 bitwise-unanimous** (cov-pin PROVEN; no N=11/quantization tax). Four degradations behind the old cloud anchors are ALL fixed: the placement lottery (cov-pin), regime-blind macro (`data/macro` baked, T-167), truncated-SPY/regime (T-167), and the `_normalize_df` load-truncation that ran the "26yr" on a ~19-ticker universe since T-082 (T-167) — plus a recent mean_variance os-crash (T-140-fu2's own `import os` shadow, swallowed by a broad except → the silent 0-trades; T-167). The 26yr WINDOW was always real (calendar = ticker-union, not SPY).

**📌 THE FIRST TRUSTWORTHY ANCHORS (CI-aware, N=5 deterministic — these REPLACE the T-155 anchors):**

| Window | canon (×5 identical) | Sharpe | ci_low | CAGR | MDD |
|---|---|---|---|---|---|
| 2022 | `eb48742e` | 1.512 | −0.59 | 31.5% | −22% |
| 16yr (2010–25) | `3e9ea427` | 1.162 | **0.676** | 10.2% | −16% |
| 26yr (2000–25) | `158fe678` | **0.751** | **0.382** | 7.3% | −33% |

**🎯 HEADLINE: the 26yr is NOT a collapse — but it's BORDERLINE, not validated.** 0.751 vs the old 0.237 = **3.2× higher**; the "collapse" was the 4-way substrate artifact, not real. BUT ci_low **0.382 sits just under the 0.40 CI-aware kill line** → not killed, not validated. **After-tax taxable = 0.093 → Roth-first re-confirmed** (taxable can't run this book). The 16yr (1.162, ci_low 0.676) clears the easy crisis-light window; the **16yr→26yr gap (1.162→0.751) is the "bull-machine" signature** — strong post-2010, borderline across the full cycle (dotcom+GFC). Direct evidence for the decompose / conditional-sleeve agenda — NOT an all-weather claim. *(Saga trail: ledger T-128-CO→T-167 + MEMORY; the old 0.237/T-092 collapse + the T-155 anchors are SUPERSEDED.)*

**STANDING RULE (kept): no cloud verdict clears a gate until its arm0 anchor reproduces at N≥5-per-window unanimity (the cov-pin now makes this bitwise by construction); arm cells must be reproducible. Every campaign carries the arm0 anchor cell as a hard gate** — it's what caught the T-155 premature stamp.

**THE FORK CONVENES on a viable-but-borderline base** (not a write-off, not validated). The levers are now cleanly testable on the trustworthy substrate: **C's T-118 de-gross overlay** (regime now LIVE → the overlay actually fires; can it lift the 26yr over the line by flattening the −33% MDD?) and **A's sleeve A/B** — both re-dispatched on the new anchors. The conditional shelf (A's T-166) is seeded for it.

**SAGA TRAIL (all RESOLVED — detail in ledger T-125→T-167 + MEMORY; do not quote as current):** the multi-week cloud-determinism saga is closed. Sequence of root causes, each found by a controlled diagnostic and superseding the last: bytecode (T-127) → "data drift" (T-126, wrong) → the placement lottery (T-128/T-140, real: cov→MVO FP nondeterminism) → hermetic/earnings contamination (T-142/T-155) → and finally the four degradations the clean re-anchor exposed (regime-blind macro, truncated SPY/regime, `_normalize_df` 90/109-ticker load-truncation, the mean_variance os-crash — all T-167) + the lottery KILLED by the deterministic cov (T-140-fu3). **Allocator-identity RESOLVED: mean_variance is production** (the Apr-23 adaptive artifact was a stale gitignored silent override — archived T-167; per T-162 it didn't outperform). **The T-155 anchors and the T-092 0.237 collapse are SUPERSEDED** by the trustworthy anchors at top.

**If this date is more than 3 days old, read `forward_plan.md` (2026-06-06 block = current plan), `health_check.md`, and the latest `docs/Audit/*` before quoting state.** Hard caps below (≤5 per section, exactly 1 next-decision) are the anti-rot discipline — when a slot fills, the oldest item moves to MEMORY or is superseded.

---

## Validated (max 5)

- **First trustworthy CI-aware anchors (T-167 re-anchor, 2026-06-14, N=5 deterministic, cov-pin proven).** 16yr (2010-25) `3e9ea427` Sharpe **1.162 / ci_low 0.676** — clears the CI-aware DSR bar (~0.66) AND MBL on its window (first cell ever to clear the formal bars) — BUT it's the crisis-light window (excludes 2008+dotcom) + survivor-biased (UPPER bound) = the **bull-machine, NOT all-weather.** The honest full-cycle test, 26yr `158fe678` **0.751 / ci_low 0.382**, is **NOT validated** (ci_low just under the 0.40 CI-aware line; SR 0.751 ≪ the ~1.55 MBL bar at honest-N) but is **NOT a collapse** (3.2× the old 0.237, which was a substrate artifact). After-tax taxable 0.093 → **Roth-only-viable.** **Net: nothing is ALL-WEATHER validated; the base is borderline-real + bull-conditional** — the decompose/de-gross agenda is about lifting a viable base over the line, not rescuing a dead one.

## Recently refuted / superseded (max 5, rolling)

- **T-092 deep-substrate baseline (FINAL, n=5/16-yr, n=4/26-yr)** — refutes "a deeper window will validate the base." Sharpe INVERTS with depth: 5yr 0.60 → 12yr 0.81 → **16yr 1.018 (ci_low 0.560, CAGR 11.0%, MDD -15.4%)** → **26yr 0.246 (ci_low -0.119, CAGR 2.64%, MDD -59.3%)**. 16-yr clears MBL + point-DSR + ci_low>0 (strongest cell ever); 26-yr FAILS every gate (8/26 years negative, worst 2008 -1.28, underperforms buy-hold SPY ~4%/yr). **Strict `ci_low > DSR-benchmark` (0.66) FAILS on EVERY window** (12yr 0.33, 16yr 0.56) — by CLAUDE.md #6 nothing is formally validated. 16-yr is the crisis-free window (excludes 2008+dotcom); base is bull-conditional. **⚠️ 16-yr leg provenance-UNVERIFIED post-T-127** (pyc-taint class; pinned re-reads ranged 0.945-1.021) — C's running campaign captures the clean 16-yr arm0 anchor; do not quote 1.018/ci_low-0.560 as precise until then (the 26-yr leg IS clean-verified: 0.237). Survivor-only → UPPER bounds. **PIVOT SIGNAL.** Also: determinism drift scales with window depth (26yr 0.19, >3-Sharpe per-year swings) — T-057c-det-followup insufficient at long windows. Branch `feature/deep-substrate-baseline-t092` @ 15bdd97. Audit: `deep_substrate_baseline_t092_2026_05_31.md`. **⚠️ SUPERSEDED 2026-06-14 (T-167 re-anchor): the 0.237/0.246 "26yr collapse" was a 4-WAY SUBSTRATE ARTIFACT — NOT real.** Clean trustworthy 26yr = **0.751 (ci_low 0.382)**, 3.2× higher (the old runs were degraded by the lottery + regime-blind macro + truncated SPY/regime + a `_normalize_df` clip that ran the "26yr" on a ~19-ticker universe). **The "collapse is real / pivot premise holds" conclusion is WRONG.** The bull-conditionality survives (16yr 1.162 > 26yr 0.751) but the gap is FAR smaller than the artifact suggested (was 1.018 vs 0.246). The base is borderline-real, not collapsed — see Validated above. *(The T-126 "0.246 re-verified" was reproducing the SAME degraded substrate 3×; reproducibility ≠ correctness.)*
- **T-098 H-Band no-trade bands** — REFUTED at ±20%/±25% on 12-yr (Δ Sharpe +0.008/+0.018, ci_low NEGATIVE both; turnover Δ <2% vs predicted −60-70%; skew mixed, arm2 worse). Band suppresses small rebalances but not the dominant large vol-target ones (Donohue-Yip calibrated to monthly concentrated books, not our daily inverse-vol 30-name). Clean Engine C impl (default-OFF, canon-OFF=baseline, ON-differs, det 3/3) kept additive for a future tighter-band sweep. Branch `feature/h-band-no-trade-t098` @ bf9da13 NOT merged (refuted+inert). Audit: `no_trade_band_h_band_t098_audit_2026_05_31.md`. **B's 12-yr arm0_off Sharpe 1.314 is MEAN-OF-12-ANNUAL, NOT comparable to pooled-full-window 0.81 — director-verified from cell JSON; 0.81 STANDS (same statistic-mismatch as the old Foundation-Gate 1.296 → pooled 0.507).**
- **T-095 fill-convention (H-Convention)** — REFUTED (good outcome): backtest already fills at **t+1 OPEN**, not close-to-close. Lou-Polk-Skouras overnight-alpha-leak does NOT apply; ~0.81 was never fill-inflated. Director-verified `execution_simulator._next_price_for_entry_exit`. Audit: `fill_convention_diagnostic_t095_2026_05_31.md`.
- **T-057 confidence-gated execution (N≥3)** — REFUTED on 12-yr (Δ Sharpe -0.128; p(Δ>0)=32%). 5-yr-Alpaca +0.793 was a substrate-conditional artifact. Do NOT flip. Audit: `multi_year_window_harness_t053b_2026_05_25.md`, `confidence_gated_flag_flip_t057b_2026_05_24.md`.
- **T-055e/g/h regime-conditional vol-target** — CLOSED on 12-yr (Δ Sharpe -0.214). 5-yr-Alpaca +0.549 "DEFENSIBLE" retired; T-055 arc monotone decay (+0.549 → +0.413 → -0.214) as rigor rose. Audit: `vol_target_12yr_verify_t055h_2026_05_29.md`.

<!-- Dropped to honor ≤5 cap (both fully captured in MEMORY.md): T-088 risk_per_trade_pct dead-knob (project_t088_risk_per_trade_dead_knob); T-087/T-089 Engine E reversal — HMM hmm_p_crisis IS predictive (AUC 0.887, 12-yr causal), the validated signal the Path-B kill-switch would use (project_engine_e_reversal_predictive). -->

> **Load-bearing for the decision above:** Engine E's `hmm_p_crisis` is VALIDATED-predictive (T-087/T-089, AUC 0.887) — it's the signal a Path-B HMM kill-switch would gate on. Full entry in MEMORY.

## In flight (max 5)

- **T-118 de-gross overlay re-run (Agent C — dispatched 2026-06-14 on the clean anchors):** the lever that could lift the borderline 26yr (0.751/ci_low 0.382) over the line by flattening the −33% MDD. Regime is now LIVE so the overlay actually fires; the δ pre-registration stands (design validated by T-118fc, no new registration). Anchor gate: arm0 == the new `158fe678`/0.751 (26yr) etc. THE headline fork input.
- **Sleeve A/B re-run (Agent A — dispatched 2026-06-14 on the clean anchors):** the spot-ETF crisis-diversifier sleeve, finally testable on a trustworthy substrate (was double-blocked on the cloud P0). Conditional-shelf entry #4.
- **B — re-anchor DONE (anchors published); FREE.** Next when the fork directs: the enable-A/B batch (T-148 buffering + T-153 vol-estimator, director pre-registers). Carryover: reconcile the **simfin foreign drift** (manifest-verify fails on simfin — find the writer; D flagged it's not theirs).
- **E — PR-3 DONE+merged; paper order-loop live (DRY-RUN/armed-paper).** Next (gated): PR-4 (archive the stub + move the deployment boundary) is HARD-GATED; the 60-day go-live clock is gated on the book/fork + arming the kill actions + one weekday fill. Designated allocator = mean_variance.
- **D — T-167 DONE+merged; cloud substrate COMPLETE; FREE.** (4 gaps fixed; the golden re-baselined; the 26yr window confirmed real.)

## Next decision (exactly 1)

- **THE FORK CONVENES — and the premise has changed: the base is NOT a collapse (0.751, not 0.237), but it is BORDERLINE (ci_low 0.382 < 0.40) + bull-conditional (16yr 1.162 vs 26yr 0.751).** The strategic question is no longer "the base is dead, pivot away" — it's "a viable-but-borderline base: can the de-gross overlay + conditional sleeves lift the full-cycle 26yr over the validation line, and is it deployable (Roth-only, taxable killed at 0.093 after-tax)?" The clean-substrate verdicts that decide it are IN FLIGHT: **C's T-118 de-gross re-run** (does it flatten the −33% MDD enough to push ci_low past 0.40?) + **A's sleeve re-run**, both on the trustworthy anchors. The conditional shelf (A's T-166) + the decompose directive (forward_plan 2026-06-11) frame the synthesis. **USER decision at the fork: the book + the deploy posture (Roth-first paper, given taxable is killed).** Honest-N keeps rising — pre-register the de-gross/sleeve reads (they already are). **Plan of record: `forward_plan.md` 2026-06-06 + 2026-06-11 + 2026-06-13 blocks** (the "base collapses" framing in older blocks is now SUPERSEDED — the base is borderline-real). Everything below this bullet is 2026-06-04 sequencing context — retained for the trail, not the current plan.
- _(historical context — 2026-06-04)_ **PATH B IN EXECUTION. MASTER FINDING (T-100, 2026-06-04): the crisis defenses EXIST in code but were STARVED in the backtest — that is why the base drew -59% on 26-yr, NOT an absence of defensive machinery.** B's instrumentation: (a) HMM **not wired** — 0/1174 advisory calls received `hmm_proba` (`hmm_enabled=false` default); (b) 5-axis regime detector **MISSED COVID** (0 crisis bars May–Dec 2020); (c) regime returns None pre-2020 on the local SPY gap (~80% of a local 26-yr run dormant); (d) the Engine-B `risk_scalar` 46% cut the audit flagged lives on **DEAD Path B** (atr-risk, confirms T-088 — prod uses Path A target_weight; the static audit-read was wrong, B's empirical trace is right). **In flight / sequenced:**
  - **T-099 (MERGED 253a96f):** long-window FP-determinism fixed (5 sites; 12-yr `--runs 3` bitwise-identical). Cloud cross-container 26-yr verify still recommended (T-099-verify).
  - **Phase 0+ (T-101 DONE 2026-06-04, Agent B):** `hmm_enabled=true` flipped in `config/regime_settings.json`. Q1' YES — posterior flows (1174/1174 advisory calls). Q2'/Q3' NO change — regime_summary distribution + per-arm gross deltas + 2022 default-cell canon are BITWISE IDENTICAL pre/post-flip. **CAPABILITY failure, not WIRING.** HMM modulates `risk_scalar` on dead Path B; Path A (production target_weight sizing) doesn't consume it. The −59% MDD was never going to be saved by a config flag. Det 3/3 PASS. Flag stays flipped (no-op for trades, observability win). Audit: `hmm_wire_phase0plus_t101_2026_06_04.md`.
  - **Phase 0b (cloud cell):** 2008 + 2000-02 need cloud data (local SPY starts 2020-04) to test crisis response on the real GFC/dotcom regimes.
  - **Phase 1 (Engine B, propose-first, 2nd gate):** binary de-gross to floor=0.25 (cash) — warranted per T-100 outcome (c), but design must REPLACE/COMPOSE-WITH the existing (now-to-be-fed) advisory de-gross, not stack a 5th cut. Returns for review.
  - **Phase 2 A/B (gated on T-099✓ + Phase-0+):** arms off/0.25/0.50/0.0/tiered, 16-yr + 26-yr, bootstrap CI. Adopt iff MaxDD −≥25% AND Sharpe-ci_low not down.
  - **Parallel fast win (APPROVED 2026-06-04):** drawdown kill-switch (already built, default-OFF, warn5/degross10/halt15) — enable + A/B. Plus capability_ledger.md + contract Layer-3 doc-fix (APPROVED). Plus F regime-conditional per-edge kill re-verify+enable (APPROVED).
  - **LAYER 2 — trend sleeve:** scaffold ALREADY EXISTS (`engine_c/sleeves/`, unwired, gauntlet-failed on megacaps) — wire+re-test, don't rebuild.
  - **Path A NOT pursued.** Deprioritized: H-Tax T-097.

## Standing constraints (max 5)

- **Honest N_trials:** `run_registry` shows **125 rows**; effective ~**260+** including cloud cells not all back-synced into the registry. MBL bar rises with N — every new measurement on the same substrate raises the threshold the next one must clear.
- **MBL at SR ≈ 0.81 base:** ~**17 years required** (Bailey-Borwein-López de Prado-Zhu 2014). 12-yr window had ~12 → borderline. 26-yr (T-092 in flight) is the test. The 5-yr substrate-honest window would require SR ≥ 1.55 to clear DSR — our base cannot.
- **5-yr-window measurements are statistically under-powered at current N** — they are exploratory only, NEVER flag-flip evidence. CLAUDE.md non-negotiable #7 codifies this.
- **Canonical substrate:** extended Stooq + Alpaca dividend-strip merged (T-082b). Survivors to 1962/1970; delisted missing pre-2020 (survivorship caveat). Any positive lift measured on a different substrate (e.g., Alpaca-only) MUST be re-verified on the canonical before flag-flip recommendation (CLAUDE.md #9).
- **Corrected baseline Sharpe ~0.81 (12-yr), ci_low ~0.33** — plausibly-real, NOT formally validated. Bootstrap CI required on every Sharpe headline (CLAUDE.md #6); kill-thresholds compare against `ci_low`, not point-estimate.

---

*Edited at session end. Hard caps per section. When content falls off the rolling cap, move to `MEMORY.md` or supersede with a newer entry. `forward_plan.md` carries the verbose, narrative version of in-flight planning; `CURRENT_STATE.md` is the at-a-glance dashboard.*
