# Capability Ledger — ArchonDEX

> **Purpose:** flat index of every BEHAVIOR-ALTERING capability the code currently ships, on which path, behind which flag, with honest reachability. This is the missing axis the decision-centric docs (CURRENT_STATE / TASK_LEDGER / MEMORY) couldn't carry: those track VERDICTS; this tracks CAPABILITY STATE.
>
> **Role:** when an A/B is REFUTED, the surviving default-off knob becomes nobody's documentation responsibility — until now. A row here for every shipped capability surfaces "what's still on the path even after this verdict."
>
> **Source:** the 2026-06-04 engine-capability-gap audit (`docs/State/health_check.md`, search "engine-auditor 2026-06-04"). T-100 (`docs/Audit/crisis_path_diagnostic_t100_2026_06_04.md`) refined the "Wired-to-live-path?" column for the Engine B Path-A-vs-Path-B sizing fork.
>
> **Authority boundary:** this file owns CAPABILITY STATE. `docs/State/CURRENT_STATE.md` owns verdicts/decisions. A refuted finding that leaves a shipped flag should point HERE for what's still on the path.
>
> **Wired-to-live-path? values:** `yes` (active on the prod-arm0 backtest path), `no` (gated off, orphaned, or on a dead sizing branch), `mode-gated` (reachable only under a specific runtime mode flip), `unknown — needs trace` (honest uncertainty; capacity not bisected).
>
> **CI-gated:** `tests/test_contracts.py::Layer 3a` verifies that every `Source (file:line)` here still resolves (file exists, symbol matches). Line-number drift WARNs; missing file/symbol FAILs. See `docs/Audit/capability_ledger_contract_layer3_t102_2026_06_04.md` for the contract spec.

> **Two systemic lessons (T-228 registry gap-hunt, 2026-06-22):**
> 1. **`data/governor/edges.yml` is the SOLE authority for edge lifecycle status.** An edge's in-code `status="active"` auto-register default is NOT authoritative: `EdgeRegistry.ensure` (`engines/engine_a_alpha/edge_registry.py:268-296`) write-protects the persisted `status` field (only Engine F's lifecycle layer may transition it, per the edges.yml Write Contract). So a code-level `status="active"` never wins for an existing edge. Any row asserting an edge is ACTIVE/RETIRED/PAUSED must cite `edges.yml`, never the in-code default. (This caught macro_yield_curve_v1 + low_vol_factor_v1, framed/self-declared "active" in code but `retired`/`paused` in edges.yml — see `[NN-NO-MANUAL-EDGES]`.)
> 2. **Run a registry gap-hunt after each merge wave — the "registry-lag pattern."** This registry (and DESIGN_FIDELITY) lagged the T-204/205/211/212/216/217/218/227 merge wave by ~one merge: 22 real-but-unregistered capabilities, a flagship CORE row (the conjunctive selector) still marked NEVER-BUILT after it shipped, and refuted-theses conflated with removed-code. The registry's whole anti-build-bias purpose fails silently when it goes stale, so refresh it (or run the gap-hunt sub-task) at the end of every merge wave, not just on a calendar. Per `[NN-AUDITS-NOT-CURRENT]`, a stale registry is not current truth.

## Engine A — Alpha (signal forecast)

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| Engine A consumes `advisory["risk_scalar"]` as a crisis de-gross brake (multiplies edge norms when regime_summary ∈ {stressed, crisis}) | A | `engines/engine_a_alpha/signal_processor.py:620` | yes | `risk_advisory_enabled` defaults True; advisory active in prod | HIGH — second crisis de-gross path in A, double-count with B per charter Matrix | health_check 2026-06-04 HIGH: A double-counts B's risk_scalar; charter says A predictive, B protective. (T-228 re-anchor: drifted :543→:620.) |
| Engine A consumes `advisory["learned_edge_affinity"]` (0.3-1.5× per-category multiplier) | A | `engines/engine_a_alpha/signal_processor.py:636` | no | Writer at `backtester/backtest_controller.py:348` is gated by `regime_conditional_enabled=false` in `config/governor_settings.json:13` | HIGH | Consumer reads `.get(..., {})` → silently `{}` in prod; Engine F producer (regime_tracker.py:180) shipped but never injected. Path-B-relevant lever for regime-conditional de-weighting. (T-228 re-anchor: drifted :563→:636.) |
| **Conjunctive "True Edge" selector** (`_conjunctive_aggregate`: s_tech × g_fund × g_regime, multiplicative AND-logic) | A | `engines/engine_a_alpha/signal_processor.py:510` (impl), `:693` (dispatch), `:546-547` (g_regime via E `hmm_regime_label`) | **mode-gated (DORMANT)** | Reachable only under `mode='conjunctive'`; prod default is weighted_sum (canon-md5 unchanged) | HIGH — the CORE intended capability; composition verdict pending the deep-window measure | T-216 (merged). Flagship DESIGN_FIDELITY row was stale (NEVER-BUILT) one merge wave; this row is the capability-state record. g_fund/g_regime gates default to 1.0 (no-op) absent populated per-edge regime data |
| **defensive_tilt screens** (T-205: `quality_tilt_longs` + `high_ivol_exclusion`) | A | `engines/engine_a_alpha/screens/defensive_tilt.py:112` (quality_tilt_longs), `:163` (high_ivol_exclusion) | **mode-gated (DORMANT) — cross-engine WIRED** | Imported + called by Engine C `phase1_composition.py:88-96` behind `phase1_composition_enabled=False` | HIGH — defensive under-participation tilt; the consumer is C/T-211 | Module's own docstring previously falsely claimed "NOT imported by the production path" — corrected in T-228 (it IS imported, flag-gated default-OFF). Cross-link: Engine-C phase1_composition row |
| `macro_yield_curve_edge` — **RETIRED in the live registry** (uniform -0.3 crisis tilt; does NOT fire in prod) | A | **`data/governor/edges.yml:1755` (`status: retired`, authority)**; `engines/engine_a_alpha/edges/macro_yield_curve_edge.py:173` (tilt logic), `:199` (in-code `status="active"`, write-protected) | **no (retired)** | edges.yml status=retired; the in-code `status="active"` default is write-protected by `EdgeRegistry.ensure` and never wins for an existing edge (see top-of-file lesson #1) | LOW — inert; T-092 Path-B kill-switch must NOT double-count this dead edge as a live de-gross path | T-228 (was framed ACTIVE / "unknown — needs trace"): edges.yml is the SOLE lifecycle authority. `[NN-NO-MANUAL-EDGES]` |
| `low_vol_factor_edge` (`low_vol_factor_v1`) — **PAUSED / retire-eligible in the live registry** | A | **`data/governor/edges.yml:1778` (`status: paused`, `tier: retire-eligible`, authority)**; `engines/engine_a_alpha/edges/low_vol_factor_edge.py:140` (in-code `status="active"`, write-protected) | **no (paused)** | edges.yml status=paused → does not contribute; in-code `status="active"` is write-protected by `EdgeRegistry.ensure` (same trap as macro_yield_curve) | LOW | T-228 (was absent from ledger; code self-declares active). edges.yml is authority. `[NN-NO-MANUAL-EDGES]` |
| `macro_credit_spread_edge` retired (status="retired") | A | `engines/engine_a_alpha/edges/macro_credit_spread_edge.py` | no | Auto-register retired (2026-05-02 reclassified as HMM input) | MEDIUM — inert ready-made defensive tilt | sibling of yield-curve edge; re-activates if a future caller loads it |
| `macro_unemployment_momentum_edge` retired | A | `engines/engine_a_alpha/edges/macro_unemployment_momentum_edge.py` | no | Auto-register retired | MEDIUM | same family as credit-spread; inert by default |
| `macro_real_rate_edge` retired | A | `engines/engine_a_alpha/edges/macro_real_rate_edge.py` | no | Auto-register retired | MEDIUM | same family as credit-spread |

## Engine B — Risk / Sizing

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| Crisis-floor on `suggested_max_positions` (5 in crisis, 7 in stressed) | B | `engines/engine_b_risk/risk_engine.py:729` (consumer); `engines/engine_e_regime/regime_config.py:105-106` (config defaults) | yes | `risk_advisory_enabled` defaults True in prod | HIGH — active de-gross knob | health_check 2026-06-04: motivated this audit, in NO living doc |
| Engine B `risk_scalar` consumption in sizing | B | `engines/engine_b_risk/risk_engine.py:739` | **no (dead Path B)** | Path B (atr_risk) sizing branch — prod uses Path A (target_weight) per T-088 | HIGH | T-100 diagnostic refined the audit's "active" claim: this consumer is on the dead atr-risk branch in prod (Engine C target_weight path actually fires). **Engine A's risk_scalar consumer (signal_processor.py:543) is the live one.** |
| `suggested_exposure_cap` consumption (de-gross) | B | `engines/engine_b_risk/risk_engine.py:736` | yes | `risk_advisory_enabled` defaults True | HIGH | Double-consumed with Engine C policy.py:380 — boundary unclear per audit |
| **`correlation_regime` sector-cap branch — gated-off producer (T-107)** | B (reader) / E (producer) | `engines/engine_b_risk/risk_engine.py:876` reader; `engines/engine_e_regime/advisory.py:254-260` GATED producer (T-107) | **no (producer gated off)** | Consumer present (reader → dynamic sector cap); producer at advisory.py:259-260 behind `correlation_regime_enabled` (regime_config.py default off) → consumer reads default "normal" | HIGH — charter-documented Matrix entries for "Elevated Correlation" / "Dispersed Correlation" reachable only if the T-107 producer flag is flipped | T-228: was "DEAD CONSUMER — no producer"; T-107 ADDED a gated producer (advisory.py:254-260), so the verdict is now "consumer wired + producer present but default-OFF", not "no producer". |
| Regime-conditional vol-target multipliers (cautious 0.85 / stressed 0.60 / crisis 0.40) | B | `engines/engine_b_risk/risk_engine.py:112` (config); `engines/engine_b_risk/vol_target.py:251` (consumer) | no | Gated by BOTH `portfolio_vol_target_enabled=False` AND `portfolio_vol_target_regime_aware=False` | HIGH — Refuted on 12-yr (T-055h) but CAPABILITY still ships | MEMORY recorded the negative VERDICT; the surviving default-off knob has no doc home until this ledger |
| Drawdown-gated kill switch (5/10/15% thresholds) — legacy Path-B-only consumer | B | `engines/engine_b_risk/risk_engine.py:83` (config), `:940` (Path-B consumer) | **no (dead Path B even when flag flipped True)** | `drawdown_kill_switch_enabled=False` default | HIGH | T-106 confirmed: even with flag enabled, halt/degrade lives inside Path-B `else:` (line 867); prod uses Path A so block never executes. T-111 PoC adds a pre-path lift behind `drawdown_kill_switch_apply_on_path_a` (default OFF). |
| T-111 PoC — Path-A lift of drawdown kill-switch (pre-path halt + Path A `target_notional` multiplier) | B | `engines/engine_b_risk/risk_engine.py:88` (flag), `:826` (pre-path block), `:884` (Path A `target_notional *= _drawdown_size_mult`) | mode-gated (PoC) | `drawdown_kill_switch_apply_on_path_a=False` default; only effective with `drawdown_kill_switch_enabled=True` | HIGH — reference implementation of the T-106 1-block lift-out fix | T-111 canon A/B: OFF == `0145c03a6496…` (≡ T-101 baseline); ON == `52202e510d27…` (DIFFERS). Determinism `--runs 3` PASS on default OFF. Director-gated A/B campaign on 16-yr + 26-yr required before any default-flag change. |
| `FactorRiskModel` (factor-neutrality) | B | `engines/engine_b_risk/factor_analysis.py:98-149` (`decompose`, T-209); `:151-163` (sizing hook, explicitly not wired) | **no — SIZING-orphaned, test-imported** | `decompose()` measurement diagnostic (T-209) is test-imported; the SIZING hook has zero prod importers | MEDIUM | T-228: was "zero importers / archive-if-unwired". T-209 added a `decompose()` measurement diagnostic (test-imported), so it is NOT fully orphaned — but the SIZING application remains unwired. Do not archive |
| Regime-conditional ATR stop-widening (high vol → wider stops) | B | `engines/engine_b_risk/risk_engine.py:59-62` (config), `:875` (consumer block) | **no (dead Path B)** | Lives in Path B (atr_risk); prod uses Path A | LOW — same dead-Path-B caveat as risk_scalar consumer | Charter Design Notes mentions "Regime-based stop widening — Keep"; reality is dead-Path-B |
| **`regime_transition_overlay` (T-118)** — HMM transition-trigger Path-A gross de-gross overlay | B | `engines/engine_b_risk/risk_engine.py:188` (flag), `:286,:392,:1041-1048,:1080` (Path-A apply); `engines/engine_b_risk/regime_transition_overlay.py` (impl) | **yes (Path A) — DORMANT** | `regime_transition_overlay_enabled=False` default (RiskConfig:188) | HIGH — refuted-as-thesis (T-118r) but the code ships fully wired on the LIVE Path A; behavior-altering when ON | T-228 untracked. "Refuted ≠ removed": this is the live-Path-A de-gross overlay still on main, inert by flag |
| **T-116 PoC — Path-A advisory `risk_scalar` de-gross lift** | B | `engines/engine_b_risk/risk_engine.py:119` (flag), `:1023-1031` (lift block), `:1080` (Path-A apply) | **mode-gated (PoC) — DORMANT** | `advisory_risk_scalar_apply_on_path_a=False` default; double-gated (needs advisory active too) | HIGH — sibling of the tracked T-111 drawdown lift; only T-111 had a row | T-228 untracked. Behavior-altering when ON (Path-A target_notional multiplier) |
| **`WashSaleAvoidance`** — tax-aware order gate (blocks new opens within wash window) | B | `engines/engine_b_risk/risk_engine.py:250` (init), `:724-748` (gate in prepare_order), `:836-842` (apply) | **no — DORMANT** | Default-OFF via `config/portfolio_settings.json:38-51` | MEDIUM — tax-channel order gate; account-dependent (Roth: no tax channel) | T-228 untracked. Gates new BUY opens when `should_block_buy` |
| **`LTHoldPreference`** — tax-aware order gate (defers sells to clear long-term-hold) | B | `engines/engine_b_risk/risk_engine.py:254` (init), `:724-748,:836-842` (gate/apply in prepare_order) | **no — DORMANT** | Default-OFF via `config/portfolio_settings.json:38-51` | MEDIUM — tax-channel order gate; account-dependent | T-228 untracked. Sibling of WashSaleAvoidance |
| **T-218 factor-neutrality SIZING** (`factor_neutrality.py` + `factor_neutrality_enabled`) | B | _NOT ON MAIN_ — branch `feature/factor-neutrality-sizing-t218` only | **branch-held — NOT on main** | Verified: no `engines/engine_b_risk/factor_neutrality.py` on main; zero `factor_neutral` matches in main risk_engine.py | MEDIUM (when merged) | T-228 BRANCH-HELD row so it can't fall through the net like the conjunctive selector did. Engine B change → propose-first before merge |
| **T-212 vol-target re-enablement build** (sigma-floor fail-loud hardening + vol-target re-enable) | B | _NOT ON MAIN_ — branch `feature/voltarget-build-t212`; main has only the non-fail-loud T-153 form (`engines/engine_b_risk/vol_target.py:286` `apply_vol_floor`) | **branch-held — NOT on main** | TASK_LEDGER T-212 = dispatched (merge HELD); main `apply_vol_floor` is the T-153 form, NOT fail-loud | MEDIUM (when merged); verdict INVALID on stale substrate, re-run pending | T-228 BRANCH-HELD row. Engine B change → propose-first before merge |

## Engine C — Portfolio Composition

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| Regime-aware vol-target upside ceiling (caps leverage to 1.0× in market_turmoil/cautious_decline/stressed/crisis; 1.4× transitional; legacy 2.0× benign) | C | `engines/engine_c_portfolio/policy.py:440` | **no (prod mean_variance returns at policy.py:292 before the adaptive branch)** | Logic at policy.py:440 (`_apply_vol_target` at :404). Prod `config/portfolio_settings.json` sets `mode: mean_variance`, whose branch returns at policy.py:199-292 BEFORE the adaptive branch; `data/research/allocation_recommendations.json` (the disk producer that could flip mode) is absent in prod → adaptive never selected | HIGH | health_check 2026-06-04 Engine C MEDIUM entry. (T-228 re-anchor :334→:440; reachability corrected mode-gated→no) |
| Vol-target downside floor (0.3× — keep ≥30% gross even in vol spikes) | C | `engines/engine_c_portfolio/policy.py:445` (`np.clip(..., 0.3, upside_ceiling)`) | **no (same mean_variance early-return as ceiling)** | Same gate as ceiling above | MEDIUM | charter (line 256) only mentions "0.3-2.0× clamp"; asymmetric/regime-aware shape invisible. (T-228 re-anchor :340→:445) |
| Engine C `_apply_exposure_cap` (consumes `advisory["suggested_exposure_cap"]`) | C | `engines/engine_c_portfolio/policy.py:463` | **no (adaptive-only; prod mean_variance does not reach it)** | Called only from the adaptive branch (policy.py:338-339), after the mean_variance early-return | HIGH — double-consumed with Engine B `risk_engine.py:736` when ON | Boundary ownership C-vs-B not stated in charter. (T-228 re-anchor :380→:463; reachability corrected mode-gated→no) |
| **`deployable_cash_account` allocator mode (T-230)** — projects MVO output to long-only (`[0, deployable_max_weight=0.25]`) + gross Σw ≤ `deployable_max_gross=1.0` (NO leverage) | C | `engines/engine_c_portfolio/policy.py:33-35` (config), `:478` (`_apply_deployable_constraints`), `:306,:359` (apply on both allocator returns) | **DORMANT (default-OFF; OFF ⇒ identity, canon byte-identical)** | `deployable_cash_account=False` default | HIGH — the honest-DEPLOYABLE config: a $5-15K cash Roth has no margin/short, so this caps the T-215-flagged un-deployable leverage (gross to 3.48×). The deployable test for any sleeve runs through this | T-242 UNTRACKED (merged b621084). Built in response to the T-215 leverage finding; the deployable verdict (vs the levered backtest) would set this ON. NB: per the T-232 finding, the levered backtest's borrow is HELD-POSITION ACCUMULATION across bars (no cross-bar cash budget), NOT MVO weights summing >1 (they sum ≤1) — see cross-cutting borrow-mechanism row |
| `MultiSleeveAggregator` (sleeve composition framework) | C | `engines/engine_c_portfolio/sleeves/aggregator.py` | **no — never wired** | Default-OFF; no caller in `BacktestController` | HIGH — Path-B Layer 2 may be partly pre-built | CURRENT_STATE Layer 2 entry didn't note this exists |
| `TrendFollowingSleeve` (CTA momentum + inverse-vol) | C | `engines/engine_c_portfolio/sleeves/trend_following_sleeve.py` | no — never wired | Default-OFF | HIGH — pre-built defensive sleeve | same as MultiSleeveAggregator |
| `MoonshotSleeve` (asymmetric-upside) | C | `engines/engine_c_portfolio/sleeves/moonshot_sleeve.py` | no — never wired | Default-OFF | MEDIUM — the UPSIDE half (now active design space) | T-242: shipped Phase A, never integrated. RELEVANT AGAIN — T-239 confirmed no buried upside half (the trend family is structurally downside-only); T-240 scoped the moonshot build; C1 (T-241) is building the highest-prior probe (CONCENTRATED conviction-weighting, NOT this sleeve specifically). Whether this scaffold is reused or replaced is the C1 design's call |
| **Trend-overlay sleeve (T-204/T-236)** — long/flat 5mo absolute-momentum overlay (the VALIDATED robo-beater / tail-protection sleeve) | C (module: core) | `core/trend_overlay.py` (`TrendOverlay`, `sleeve_returns`, pure module); consumed by `engines/engine_c_portfolio/phase1_composition.py:88-114` (DORMANT) | **DORMANT in code (composition default-OFF); VALIDATED offline as a STANDALONE sleeve** | `phase1_composition_enabled=False` default; standalone-sleeve deploy is the forward path (paper E/T-238 proposal, nothing enabled) | HIGH — the first validated robo-beater on the TAIL yardstick (T-236: Sortino 1.085, MaxDD −12%, survives dotcom). The deploy lever is the SLEEVE, not the equity book (H0, T-215) | T-242 UNTRACKED as the standalone deploy candidate. Cross-link: Engine-C phase1_composition row + DESIGN_FIDELITY trend-sleeve row. NOT yet a wired standalone allocation (validated in analysis; → paper) |
| `EngineCAllocator` (charter + index.md reference) | C | _missing — file correctly absent_ | **n/a — RESOLVED** | File still does not exist (correct) AND the original premise is stale: charter + `index.md` no longer reference an `allocator.py` FILE. The only remaining `allocator`/`Allocator` hits are generic role nouns (`engine_charters.md:17` "Portfolio Manager / Allocator"; `index.md:61` "...policy allocator"), not a dangling file reference | LOW | T-228: the file-level dangling reference is gone; row kept as RESOLVED record |
| `allocation_recommendation` consumer in policy | C | `engines/engine_c_portfolio/policy.py:129` (consumer); `:131-147` (disk fallback) | **consumer wired; disk fallback INERT in prod** | Consumer reads `advisory.get('allocation_recommendation')` at :129; `load_recommendations()` disk fallback at :132-147 (try/except → empty) — `allocation_recommendations.json` absent in prod, so adaptive mode is never selected | LOW | T-228: resolved "unknown — needs trace" (:62→:129). Engine-F producer `AllocationEvaluator` exists but `auto_apply_allocation=false` → no prod artifact written. Cross-link: Engine-F allocation-evaluation producer row |
| **phase1_composition post-processor (T-211)** — composes A/T-205 defensive tilt + E/T-204 trend-overlay LAST in compute_target_allocations | C | `engines/engine_c_portfolio/phase1_composition.py:114` (`apply_phase1_composition`); `engines/engine_c_portfolio/portfolio_engine.py:440-441` (gated call), `:587-608` (`_apply...`) | **yes (live path) — DORMANT** | `phase1_composition_enabled=False` default | HIGH — the live consumer of A's defensive_tilt screens + E's trend-overlay | T-228 untracked. Cross-link: Engine-A defensive_tilt row |
| **Carver dynamic optimization (T-139)** — integer-position layer in compute_target_allocations | C | `engines/engine_c_portfolio/dynamic_optimizer.py` (`optimize_integer_positions`); `engines/engine_c_portfolio/portfolio_engine.py:424` (gated call), `:510-585` (impl) | **yes (live path) — DORMANT** | `dynamic_optimization_enabled=False` default (OFF == canon-unchanged) | MEDIUM | T-228 untracked |
| **Carver position buffering (T-148)** — 10% trade-to-edge inertia in compute_target_allocations | C | `engines/engine_c_portfolio/position_buffering.py` (`apply_position_buffering`); `engines/engine_c_portfolio/portfolio_engine.py:433` (gated call), `:469-508` (impl) | **yes (live path) — DORMANT** | `position_buffering_enabled=False` default | MEDIUM — turnover/tax lever (T-148: tax channel ~29× cost) | T-228 untracked |
| **PortfolioComposer (HRP + turnover gate)** — C-owned, A-dispatched | C | `engines/engine_c_portfolio/composer.py:102` (`compose`), `:99-100` (`is_active`); constructed/called from Engine A `engines/engine_a_alpha/alpha_engine.py:571` (construct), `:799-800` (call) | **reachable on live path; INERT under prod `method=weighted_sum`** | Prod `portfolio_optimization` config uses `weighted_sum` → HRP/turnover-replacement path not taken | MEDIUM — C-owned code dispatched from A (boundary note) | T-228 untracked |
| **PortfolioComposer HRP-replacement slice-1 (`method='hrp'`)** — FALSIFIED | C | `engines/engine_c_portfolio/composer.py:20-25` (docstring: "method=hrp → HRP-as-replacement (slice 1 — FALSIFIED) ... Sharpe regression −0.63") | **no (refuted; code retained)** | Reachable only under `method='hrp'`; prod is `weighted_sum` | LOW — refuted-thesis but surviving capability | T-228: split refuted-thesis (−0.63 Sharpe vs weighted_sum) from removed-code — the method STILL SHIPS in composer.py, inert. "Refuted ≠ removed" |
| **SpotETFTrendSleeve (T-120)** — spot 8-ETF crisis-diversifier capital-partition sleeve | C | `engines/engine_c_portfolio/sleeves/spot_etf_trend_sleeve.py` (`SpotETFTrendSleeve`); `engines/engine_c_portfolio/portfolio_engine.py:79` (init capital partition), `:322-331` (snapshot equity PnL) | **wired into PortfolioEngine init + equity — DORMANT** | `spot_etf_trend_sleeve_enabled=False` default | HIGH — HEDGE THESIS REFUTED (T-128r) but CODE STILL SHIPS WIRED, inert | T-228: DESIGN_FIDELITY blanket-"sleeve REFUTED" row conflated refuted-with-removed; this is the surviving wired capability. "Refuted ≠ removed" |

## Engine D — Discovery

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| Gate 0 — MBL pre-flight (`mbl_sr_target=1.0` default) | D | `engines/engine_d_discovery/discovery.py:869` (validate_candidate entry) | yes (`--discover` path) | Default ON | LOW | Charter claims "4-Gate" pipeline; actual code runs 8 gates |
| Gate 5 — Universe-B production-equivalent transfer | D | `engines/engine_d_discovery/discovery.py` (gate 5 block within validate_candidate) | yes (`--discover` path) | Default ON | LOW | Wired live in `orchestration/mode_controller.py:1209` |
| Gate 6 — FF5+Mom factor-alpha (t>2 AND α>2%) | D | `engines/engine_d_discovery/discovery.py:1552` (gate 6 block) | yes (`--discover` path) but **DIAGNOSTIC-ONLY in prod (cannot kill)** | `factor_gate_mode="report"` in `config/discovery_settings.json` (T-203) → gate STILL COMPUTES the HAC-corrected FF5+Mom t-stat but `factor_alpha_passed_for_gate` is forced pass; set `factor_gate_mode="kill"` to restore the kill behavior | LOW — no longer enforces factor-orthogonal alpha at promotion (T-203 re-aim to beat-the-robo, not academic factor-α) | T-228: corrected "ENFORCING" → diagnostic-only-in-prod. T-043 was a SIBLING gate at retirement; Gate 6 is at promotion |
| Gate 7 — Substrate-transfer drift (historical S&P 500) | D | `engines/engine_d_discovery/discovery.py` (gate 7 block) | yes (`--discover` path) | Default ON | LOW | Path-B-relevant for substrate-honest validation |
| Gate 8 — DSR multiple-testing deflation | D | `engines/engine_d_discovery/discovery.py` (gate 8 block) | yes (`--discover` path) | Default ON | LOW | Project's accumulated-N reflated benchmark applied at promotion |
| Macro genes — `vix_level`, `yield_curve`, `unemployment_delta` (10% emit rate) | D | `engines/engine_d_discovery/discovery.py:496` (`_create_random_gene`) | yes when `--discover` fires | Default ON in GA | HIGH — Engine D ALREADY capable of discovering crisis-aware edges | Resolved by `engines/engine_a_alpha/edges/composite_edge.py:181` macro handler |
| Behavioral genes — `panic_score`, `herding_breadth` (5% emit rate) | D | `engines/engine_d_discovery/discovery.py:496` | yes when `--discover` fires | Default ON in GA | MEDIUM | resolved by composite_edge behavioral handler |
| Regime genes — `is bear` (5% emit rate) | D | `engines/engine_d_discovery/discovery.py:496` | yes when `--discover` fires | Default ON in GA | HIGH | regime-conditional edge discovery, Path-B-relevant |
| Short / market-neutral direction emission (10% / 10% emit rate) | D | `engines/engine_d_discovery/discovery.py:353-357`; `engines/engine_d_discovery/genetic_algorithm.py:157-163` | yes when `--discover` fires | Default ON in GA | HIGH — hedge/short edge discovery is existing capability lever | Resolved by composite_edge sign at line 150 |
| **Foundry-feature gene bucket (T-022)** — LARGEST single GA gene bucket (20% emit, roll<0.85) | D | `engines/engine_d_discovery/discovery.py:657-675` (Foundry 20% bucket), `:455` (`_make_random_foundry_gene`); `engines/engine_a_alpha/edges/composite_edge.py:56-71` (gene refresh) | yes when `--discover` fires | Default ON in GA | HIGH — largest GA gene bucket; was thought inert from the T-177 `set_params` bug, now LIVE | T-228 untracked. Previously inert via the T-177 bug (composite genes=[]); T-177 fix made it live |
| **`foundry_seed_fraction` (T-183)** — fraction of fresh Gen-0 GA seed allocated to single-gene foundry genomes | D | `engines/engine_d_discovery/discovery.py:89` (dataclass default 0.0), `:96-102` (reads config); `config/discovery_settings.json` (`foundry_seed_fraction: 0.5`) | yes when `--discover` fires | **Default 0.0 in dataclass but PROD config = 0.5** (default-ON in prod) | MEDIUM — behavior-altering knob; half the Gen-0 seed is single-gene foundry genomes in prod | T-228 untracked. Seed diversity only; gauntlet/DSR gates unchanged |
| **`use_bayesian_opt` (T-028)** — BayesianOptimizer (skopt GP+EI) candidate-search selector replacing the GA | D | `engines/engine_d_discovery/discovery.py:268-283` (reads flag), `:285-293` (routes to BayesianOptimizer); `config/discovery_settings.json` (`use_bayesian_opt: false`) | mode-gated (`--discover`) | `use_bayesian_opt=false` default-OFF; now CONFIG-REACHABLE (config file exists) | MEDIUM | T-228 untracked. MEMORY had noted it inert when no config existed; config now exists so it is reachable |
| **`robo_deploy_gate_enabled` (T-203)** — config claims `evaluate_deploy_readiness` is the "PRIMARY deploy-readiness gate" | D | `config/discovery_settings.json` (`robo_deploy_gate_enabled: true`, `_factor_gate_comment`); `core/combined_candidate_scorecard.py` (`evaluate_deploy_readiness`); `scripts/run_combined_scorecard.py` (sole non-test caller) | **PHANTOM-as-described** — config-claims-vs-wiring gap | The config key has ZERO python consumers (grep finds it only in the config file); `evaluate_deploy_readiness` is wired ONLY into the offline scorecard script + its test — NEVER into `validate_candidate` or the autonomous `--discover` cycle | HIGH — a registry/config asserting a load-bearing gate that the autonomous path never runs is the exact failure the anti-build-bias tool exists to catch | T-228 PHANTOM. It is an OFFLINE / director deploy-readiness gate (run manually via `scripts/run_combined_scorecard.py`), NOT an autonomous discovery-cycle gate. Config comment corrected in T-228; NOT wired into discovery (out of scope) |

## Engine E — Regime Detection / Advisory

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| Advisory output dict (regime_summary / risk_scalar / suggested_exposure_cap / suggested_max_positions / edge_affinity / caution_note / regime_confidence) | E | `engines/engine_e_regime/advisory.py:242` | yes | `risk_advisory_enabled` defaults True; advisory built every bar | HIGH — primary cross-engine defensive contract surface | Layer 3b test enforces reader⊆writer |
| `regime_transition_warning` advisory extension | E | `engines/engine_e_regime/regime_detector.py:246` | yes | Set when transition warning fires | LOW | Read-only diagnostic; no consumer branches on it |
| Multi-resolution advisory blend (`multires_advisory`) | E | `engines/engine_e_regime/regime_detector.py:244` (`advisory.update(multires_advisory)`) | yes if multi-resolution detector active | mode-dependent | LOW | Surfaced into advisory but consumer-side check is opaque |
| HMM Variant C (minimal_c feature_set; 4 long-history FRED + hyg_ig_oas + copper_gold_ratio + xlp_xly_ratio) | E | `engines/engine_e_regime/regime_config.py:121` (config); model artifact `engines/engine_e_regime/models/hmm_minimal_C_v1.pkl` | no | `hmm_enabled=true` since T-101 (2026-06-04) BUT prod `config/regime_settings.json:99-105` selects `feature_set="legacy"` — minimal_c not selected | HIGH — Path-B HMM kill-switch foundation | T-087 validated AUC 0.887 on 12-yr; T-101 testing |
| HMM Variant A/B (legacy + minimal_a/b feature sets) | E | `engines/engine_e_regime/regime_config.py:121` (enum) | posterior computed every bar; consumed by nothing that sizes a trade (T-101: bitwise-identical trades pre/post flip) | `hmm_enabled=true` since T-101; prod loads `hmm_3state_v1.pkl` (legacy) | MEDIUM | Multiple model artifacts shipped: `hmm_3state_v1.pkl`, `hmm_minimal_A_v1.pkl`, `hmm_minimal_B_v1.pkl`, `hmm_3state_vix_term_v1.pkl`, `hmm_monthly_v1.pkl`, `hmm_weekly_v1.pkl` |
| **Validated crisis HMM (`hmm_3state_crisis_v1.pkl`, T-103/T-105: OOS AUC@5d 0.914-0.919, combined posterior)** | E | model artifact `engines/engine_e_regime/models/hmm_3state_crisis_v1.pkl`; referenced ONLY by `Archive/scripts/train_hmm_crisis_t103.py`, `scripts/validate_hmm_*_t10{3,5}.py`, `scripts/gen_t118_campaign_spec.py` | **no — never loaded by production** (`config/regime_settings.json:101` points at legacy `hmm_3state_v1.pkl`, the AUC-0.49 false-negative model T-087 reversed) | Repoint is propose-first; T-118 drives its overlay with the crisis model; if it wins, production repoint rides along (separate gate) | **HIGH — the project's single validated predictive signal is not the model production runs** | Flagged by 2026-06-11 fresh-view review; previously discoverable only inside the T-118 audit doc |
| **`hmm_regime_label` (T-217)** — causal-HMM 3-state {calm/cautious/crisis} label (fail-safe calm) | E | `engines/engine_e_regime/regime_gate.py:63`; imported + called at `engines/engine_a_alpha/signal_processor.py:546-547` | **mode-gated (DORMANT)** | Reachable only under A's `mode='conjunctive'` (default-OFF); on the live IMPORT path | HIGH — the validated-label half of A's conjunctive g_regime gate (repoints causal p_crisis, NOT the net-negative 5-axis advisory) | T-228 untracked. Cross-link: Engine-A conjunctive selector row + DESIGN_FIDELITY hmm_p_crisis row |
| **RegimeGate per-(edge,regime) overlay subsystem (T-217)** — `RegimeGate` + `build_gates_from_stats` / `gate_from_sharpe` | E | `engines/engine_e_regime/regime_gate.py:108` (`RegimeGate`), `:77` (`gate_from_sharpe`), `:87` (`build_gates_from_stats`) | **no — ZERO production importers** | Built scaffold, never wired; no on-disk artifact (no re-labelable per-edge-per-regime data) | LOW — inert scaffold; the validated-label half (hmm_regime_label) IS wired, this overlay subsystem is not | T-228 untracked. T-196 H0 near-edgeless book → likely stays correctly OFF |

## Engine F — Governance / Lifecycle

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| `RegimePerformanceTracker.get_learned_affinity()` → advisory["learned_edge_affinity"] | F | `engines/engine_f_governance/regime_tracker.py:180` (producer); `backtester/backtest_controller.py:368` (injector) | **no (inert in prod — verdict correct, GATE corrected)** | Injector is gated by `learned_affinity_enabled` (=true, `config/governor_settings.json:16`) — NOT `regime_conditional_enabled`. Inert in prod via the EMPTY tracker (no learned affinities accumulated), not via that flag | HIGH — Path-B lever for regime-conditional crisis de-weighting | T-228: corrected the stated gate (was `regime_conditional_enabled=false`; the real gate is `learned_affinity_enabled`, which is true — inert comes from the empty tracker). Injector :348→:368 |
| Factor-α retirement gate (T-043) | F | `engines/engine_f_governance/lifecycle_manager.py:614`; `engines/engine_f_governance/factor_alpha_gate.py` | **no — call-site bug** | `factor_alpha_enabled=True` but production caller `governor.evaluate_lifecycle` doesn't pass `factors=`, so the body short-circuits | HIGH | Charter Invariant 4 ("Edge demotions require statistically significant underperformance") — invisibly inert in autonomous loop |
| `regime_conditional_enabled` regime-conditional weight blending | F | `engines/engine_f_governance/governor.py` (`_rebuild_regime_weights_from_tracker`, `get_edge_weights(regime_meta=)`) | no | `regime_conditional_enabled=false` in prod | HIGH — same flag as learned_edge_affinity above | Path-B lever |
| `RegimePerfAnalytics` module reference | F | _missing_ | n/a | Charter + `engines/engine_f_governance/index.md:15` reference file that does not exist | LOW | Resolved by `scripts/sync_docs.py` regeneration or restoring from Archive |
| **Autonomous lifecycle transitions** (active→paused→retired→revive) directly mutating `data/governor/edges.yml` | F | `engines/engine_f_governance/lifecycle_manager.py:26`; gated in `engines/engine_f_governance/governor.py:620` (`update_from_trade_log`); `orchestration/mode_controller.py:1043` (fires) | **yes — ACTIVE in prod** | dataclass default `lifecycle_enabled=False` (governor.py:61) BUT prod `config/governor_settings.json:19 lifecycle_enabled=true`, `:23 lifecycle_readonly=false` → WRITES edges.yml every prod run | **HIGH — the single most consequential F behavior; the ONLY thing that legitimately mutates edge lifecycle status (see top-of-file lesson #1)** | T-228 untracked at capability granularity (only a system-level DESIGN_FIDELITY row existed). This is why edges.yml is the lifecycle authority — F owns it autonomously |
| **Allocation-evaluation producer** (`AllocationEvaluator` → `allocation_recommendations.json`) | F | `engines/engine_f_governance/governor.py:558-562` (constructs evaluator in `update_from_trade_log`); `config/governor_settings.json:17` (`allocation_evaluation_enabled=true`), `:18` (`auto_apply_allocation=false`) | **yes — ACTIVE producer (output not auto-applied)** | `allocation_evaluation_enabled=true`; `auto_apply_allocation=false` → evaluates (384 param combos) but does NOT write a prod artifact C reads | MEDIUM — the F-side producer of the C-side `allocation_recommendation` consumer (only the consumer was tracked) | T-228 untracked. Cross-link: Engine-C allocation_recommendation row |
| **MDD −25% kill-switch / pause gate** | F | `docs/Core/engine_charters.md:481,:488` (Design Note); `engines/engine_f_governance/lifecycle_manager.py:35` (MDD-spike trigger: rolling-90d MDD < `pause_mdd_threshold`) | **reachable — gated by `lifecycle_enabled=true`** | Same gate as autonomous lifecycle (prod `lifecycle_enabled=true`) | HIGH — charter §F promises immediate pause on −25% MDD; reachable when lifecycle is on | T-228 untracked. Safety mechanism, not a performance judgment (charter §F) |
| **Tier reclassification (`governor.evaluate_tiers`)** — FF5+Mom decomp mutating edge `tier` in edges.yml | F | `engines/engine_f_governance/governor.py:665` (`evaluate_tiers`), `:78` (`tier_reclassification_enabled` default False), `:690` (early-return guard); `orchestration/mode_controller.py:1056` (call site) | **no — DORMANT (default-OFF)** | `tier_reclassification_enabled=False` (dataclass default; not set in governor_settings.json) | MEDIUM — behavior-altering when ON (mutates edges.yml tier) | T-228 untracked |
| **T-227 runtime dead-gate assert (`governor._assert_regime_weight_keys_reachable`)** | F | `engines/engine_f_governance/governor.py:189-232`; called at `:187` (`__init__`) and from `update_from_trades` (`:435`) | **yes (wired) — defensive/observability** | Always runs; HALTs in measured mode on unreachable `_regime_weights` keys | LOW — fail-closed guard, not a sizing capability, but CAN HALT a measured run | T-228 untracked (merged commit 837b887). `tests/test_governor_regime_weight_*` lock it |

## Cross-cutting

| Capability | Engine | Source (file:line) | Wired-to-live-path? | Prod-flag-state | Defensive/Path-B relevance | Notes |
|---|---|---|---|---|---|---|
| `enforce_target_allocations=True` makes Engine B Path A (target_weight) live; Path B (atr_risk) is dead in prod | B | `engines/engine_b_risk/risk_engine.py:51` (config), `:817` (Path A); `:867` (Path B) | Path A: yes / Path B: **no** | `enforce_target_allocations=True` default; ALSO `target_weights` always passed by controller in prod | foundational — many "active" capabilities in audit actually sit on Path B and are dead in prod | T-088 found this; T-100 quantified it; affects every Engine B sub-capability whose code lives in lines 867+ |
| **Backtest leverage / "borrow" mechanism — T-232 FINDING (corrects the T-215 attribution)** | B | `engines/engine_b_risk/risk_engine.py` (held-position accumulation across bars; no cross-bar cash-budget check) | **ACTIVE in the levered backtest (gross to 3.48× T-215); deployable cap = C `deployable_cash_account` (OFF)** | no flag — structural; the un-deployable leverage T-215 flagged | HIGH — the levered T-215/base MaxDD (−63.5%) is a borrow artifact; un-deployable in a cash Roth | **T-242 CORRECTION:** the borrow is **held-position accumulation across bars** (positions held + added without a cross-bar cash budget), NOT per-name MVO sizing (which T-215 mis-attributed; MVO weights sum ≤1). T-232 fix is **HELD** (Engine B, propose-first — not on main). The deployable projection lives in C's `deployable_cash_account` (above) |

## How to add a row

1. Identify a behavior-altering capability — code that, when toggled, changes a backtest trade or live order. Pure logging, doc strings, or test scaffolding are NOT capabilities; do not add rows for them.
2. Fill all 7 columns. `Source (file:line)` must resolve — Layer 3a is gated CI.
3. For `Wired-to-live-path?`, do the 3-way join: config-flag × wiring-guard × path-reachability. Use `unknown — needs trace` if any leg is unverified. False precision is the bug this ledger exists to prevent.
4. Cross-link a verdict doc that refutes the capability's lift but leaves the knob alive (e.g., T-055h refuted vol-target but `portfolio_vol_target_crisis_multiplier=0.40` ships).

## Coverage stat

- Rows: ~57 (HIGH-defensive: ~16; Path-B-relevant marked). +23 in the T-228 registry gap-hunt (2026-06-22) closing the T-204/205/211/212/216/217/218/227 merge-wave lag — incl. the always-ON F autonomous-lifecycle machinery, A's conjunctive selector, C's phase1/dynamic-opt/buffering/sleeve, D's Foundry buckets + the robo_deploy_gate PHANTOM, and 2 branch-held B builds (T-218/T-212). **+4 in the T-242 lighter currency pass (2026-06-26)** closing the T-215/230/234/236/239/240 wave: the `deployable_cash_account` allocator mode (T-230, was UNTRACKED), the validated trend-overlay STANDALONE sleeve (T-236, the realized robo-beater), the borrow-mechanism CORRECTION (T-232: held-position accumulation, NOT per-name sizing — supersedes the T-215 attribution), and the re-activated MoonshotSleeve design space.
- **Strategic-currency note (T-242):** the equity book is **H0** (T-215, on Sharpe AND Sortino); the realized robo-beater is the **trend-overlay sleeve** (T-236, → paper); the measure is **Sortino/tail** (2026-06-25 directive); `[NN-AI-GATE]` governs the LLM/catalyst frontier. In-flight: C1 concentration probe (T-241), paper validation (T-238), Lazy-Prices (T-237, leaning H0). The conjunctive selector (T-216) remains BUILT+DORMANT but is no longer the live CORE prize (it would refine an H0 book) — see DESIGN_FIDELITY strategic-reality update.
- Source: 11 engine-auditor 2026-06-04 entries in `docs/State/health_check.md` + the T-228 6-engine registry gap-hunt (2026-06-22).
- Authority: this file is the canonical CAPABILITY INDEX. Verdicts/decisions remain in CURRENT_STATE.md. Edge lifecycle status authority = `data/governor/edges.yml` (see top-of-file lesson #1).
