---
task_id: T-2026-06-16-174
title: VRP (variance risk premium) edge — equity-implementable scope + backtest (the lone remaining alpha candidate)
date: 2026-06-16
scope: research/backtest only (no promotion, no edge_weights.json, no Engine-B sizing change); free data only
data: ^VIX + ^GSPC via yfinance (1995-2025, pinned CSV under data/external/vrp/); base book = canonical 26yr arm0 158fe678 (data/external/base_curve/t118r_v1_26yr_arm0_3b403882.csv)
status: CURRENT (pre-registration committed before running — see git history; results appended after)
outcome: "**VRP is NOT a harvestable equity edge in our form — REFUTED, fails the pre-registered ci_low>0 gate in every overlay.** PRIMARY (VRP vs expanding-median, w=0.5) HURTS: Sharpe 0.751→0.499, ΔSharpe −0.252 ci [−0.371,−0.124] (whole CI < 0); least-bad is VRP<0 panic-de-risk Δ −0.063 ci_low −0.156 (still fails). Overlays cut MDD modestly (−32.6%→−27.6/−29.7%) but the gate was Δ Sharpe ci_low, which fails — and the bought-MF sleeve is a better crisis lever. The implied-vol (VIX) term adds no timing value the realized-vol overlays (vol-target, closed) lacked. The pure VRP premium IS real but needs an options desk (propose-first capability, not an equity finding). Closes VRP-in-our-form → joins cross-sectional alpha (0/16) on the closed pile; no equity-implementable alpha candidate left untested; a genuinely NEW edge still owed to beat the robo. N_trials += 1. Net of 10bps/|Δw|; fails gross too (timing, not cost)."
---

# T-174 — VRP Edge (Equity-Implementable)

## 1. PRE-REGISTRATION (committed before any result)

### 1.1 What VRP is, and the honest limit up front

The variance risk premium = implied variance persistently exceeds
realized variance (VIX² > E[RV²]); the classic harvest is **short
variance** (sell options / variance swaps, collect IV−RV). **We are
NOT adding an options desk** — so the pure premium is NOT implementable
here. What an equity-only system CAN use is the **return-prediction
content** of VRP (Bollerslev-Tauchen-Zhou 2009: a rich VRP forecasts
higher forward equity returns; a compressed/negative VRP — realized vol
overtaking implied — flags poor forward returns / crisis onset). So the
testable equity form is a **VRP exposure-timing overlay on the base
book**, NOT a variance harvest. This must be stated as the ceiling: we
can only capture VRP's (weaker, regime-dependent) timing signal.

**Novelty vs what's already closed:** vol-target (T-055, closed
negative on 12-yr) times on REALIZED vol only. VRP adds the
forward-looking IMPLIED-vol (VIX) term. So the precise question is:
**does the implied-vs-realized spread (VRP) time the book's exposure
better than zero, net of cost — i.e. does VIX's forward-looking
information add harvestable timing value the realized-only overlays
lacked?**

### 1.2 Signal (fixed, causal)

- `RV21_t` = trailing 21-trading-day realized vol of ^GSPC daily log
  returns, annualized (×√252), in vol points (%).
- `VRP_t = VIX_t − RV21_t` (vol-point spread; positive = implied richer
  than realized = the normal premium state).
- All signal values at `t` use data through `t` only; the overlay
  weight `w_t` is applied to the base book's return at `t+1` (no
  lookahead).

### 1.3 Overlay (fixed — single canonical primary)

- **PRIMARY:** `w_t = 1.0` if `VRP_t ≥ M_t` else `0.5`, where `M_t` is
  the EXPANDING-window median of VRP up to `t` (no look-ahead, no free
  threshold parameter). Economic reading: when risk compensation is at
  or above its own historical norm, stay fully invested; when VRP is
  compressed/negative (panic, IV no longer cushioning realized), halve
  exposure. Applied: `r_overlay_{t+1} = w_t · r_base_{t+1} − cost·|w_t − w_{t−1}|`.
- **Cost:** exposure changes trade the book by `|Δw|`; modeled at
  **10 bps round-trip per unit |Δw|** (5 bps/side — the project's
  realistic-model mega-cap assumption; conservative for a broad book).
- **Sensitivities (reported, NOT the primary; N+=0):** `w_low ∈ {0.0, 0.5}`
  and a `VRP_t < 0` (sign-cross) trigger — to show robustness, not to
  select.

### 1.4 Substrate + gate (fixed)

- Substrate: the trustworthy re-anchored base book, daily, 2000-2025
  (where VIX+GSPC overlap the base — full 26yr). VRP needs ~1yr warmup
  for the expanding median → effective test ~2001-2025.
- Metric: Δ Sharpe (overlay − base) with **block-bootstrap ci_low**
  (block=Politis-White auto, n_iter=1000, seed=42, per CLAUDE.md #6),
  plus MDD and CAGR. MBL-aware: a 24-yr daily window clears MBL for
  any SR the overlay could plausibly add.
- **GATE (pre-registered): the VRP overlay is a real harvestable edge
  iff Δ Sharpe `ci_low > 0` NET-of-cost on the deep substrate.** Point
  Δ > 0 with ci_low ≤ 0 is NOT a pass (CLAUDE.md #6). Per #9 the
  verdict is on this substrate.

### 1.5 N-trials

**N_trials += 1** (one canonical pre-registered signal). The
sensitivities are robustness reads on the same hypothesis, not new
trials. Pre-registered before unblinding per CLAUDE.md #7.

### 1.6 Expected outcome (pre-stated, to avoid post-hoc spin)

Prior is skeptical: VRP's return-prediction is real but weak and
concentrated at monthly+ horizons; as a daily equity overlay net of
cost it may not clear ci_low > 0 — like the realized-vol overlays
before it. A pass would be the first new harvestable edge post-fork; a
fail closes VRP-in-our-form (the honest, likely outcome) and leaves
the bought-MF sleeve (T-170/171/173) as the crisis lever and a NEW
edge still owed.

---

## 2. RESULTS

(Appended after the pre-registration commit — verify section 1 predates these numbers in git history. Reproducible: `scripts/vrp_edge_t174.py`.)

Aligned base+VRP daily window: 6,538 days (2000-01-04→2025-12-31).
VRP mean +3.45 vol-points, positive 84% of days (the premium state is
the norm; VRP<0 = the ~16% panic tail). Base-alone: Sharpe **0.751**,
MDD −32.6% (matches the canonical 26yr anchor).

| Overlay (net of 10bps/|Δw|) | Sharpe | Δ Sharpe | Δ ci [lo, hi] | MDD | Gate |
|---|---|---|---|---|---|
| **PRIMARY: median, w=0.5** | 0.499 | **−0.252** | **[−0.371, −0.124]** | −27.6% | **FAIL** |
| median, w=0.0 | 0.052 | −0.699 | [−0.993, −0.401] | −29.1% | FAIL |
| VRP<0 (sign), w=0.5 | 0.689 | −0.063 | [−0.156, +0.031] | −28.0% | FAIL |
| VRP<0 (sign), w=0.0 | 0.569 | −0.182 | [−0.375, +0.019] | −29.7% | FAIL |

### 2.1 Verdict — VRP is NOT a harvestable equity edge in our form (REFUTED)

**Every overlay form fails the pre-registered gate (Δ Sharpe ci_low > 0
net-of-cost).** The PRIMARY median-overlay HURTS materially (Sharpe
0.751 → 0.499; Δ −0.252, ci_low −0.124 — entire CI below zero): sitting
at half-exposure whenever VRP is below its own median (~half the days
by construction) sacrifices too much of the book's bull return. The
least-bad form is the VRP<0 sign-cross panic-de-risk (Δ −0.063, ci_low
−0.156) — economically nearly free, but still NOT additive and still
fails ci_low > 0.

**The one consolation, which does not change the verdict:** every
overlay cuts MDD modestly (−32.6% → −27.6% to −29.7%) — VRP's panic
signal does provide *some* drawdown defense. But (a) the gate was
Δ Sharpe ci_low, which fails, and (b) we already have a better-
characterized crisis lever (the bought-MF sleeve, T-170/171/173). VRP
adds no harvestable Sharpe edge over the base, and the implied-vol
(VIX) information adds no timing value the realized-vol overlays
(vol-target, closed) lacked.

**This closes VRP-in-our-form** — joining cross-sectional alpha
(0/16) on the closed-evidence pile. The honest implication for the
fork: there is now **no equity-implementable alpha candidate left
untested** that we know of. The crisis-defense lever (bought MF) is
real-but-partial (T-173); a genuinely NEW edge — likely requiring a
capability we don't have (options for a true VRP harvest, or a new
data modality) — is still owed to clear the user's beat-the-robo bar.
The pure VRP premium IS real and well-documented; harvesting it needs
an options desk, which is a propose-first capability decision, not an
equity-backtest finding.

### 2.2 Caveats

- Tests only VRP's return-prediction content (the equity-implementable
  part); the short-variance harvest (the actual premium) is out of
  scope by construction (no options desk).
- Single canonical pre-registered overlay + 3 robustness sensitivities;
  no threshold was tuned to the result (the expanding median + the 0
  sign-cross are parameter-free).
- Net of a conservative 10bps/|Δw| cost; even gross (cost-free) the
  primary Δ stays negative (the failure is the timing, not the cost).
- N_trials += 1.
