# `g_regime` Gate — DELIVERY + honest read (T-217, 2026-06-18)

Reads against the pre-registration (`regime_gate_preregistration_t217`,
locked `da51943`). Branch `feature/regime-substrate-t217`.

## What shipped (the composable input A's T-216 selector consumes)
`engines/engine_e_regime/regime_gate.py` — Engine E's `g_regime` gate,
**default-OFF, canon-safe**:
- **`hmm_regime_label(regime_meta)`** — a clean 3-state regime label
  (calm/cautious/crisis) from the **validated, causal** HMM `p_crisis`
  (T-087/089, AUC 0.887) carried in `regime_meta["hmm_regime"]` — the RANK-3
  capability that was consumed by nothing live. It deliberately **does NOT
  read `advisory["regime_summary"]`** (the 5-axis label that net-negatived
  the walk-forward, RANK 2). Fail-safe to `calm` when the HMM is absent.
- **`gate_from_sharpe` / `build_gates_from_stats`** — the pre-registered
  Sharpe→gate map (no sweep) that turns measured per-(edge, regime)
  performance into `{edge: {regime: multiplier∈[0,1]}}`.
- **`RegimeGate.gate(edge, regime_meta) → float`** — the contract. `gates={}`
  == OFF == 1.0 everywhere (a bitwise no-op).
- 10 unit tests; **canon literally unchanged** (no prod path imports the
  module — it's a dark, opt-in input until A composes it).

## The contract for A (proposed; none existed on-disk)
```python
from engines.engine_e_regime.regime_gate import RegimeGate
rg = RegimeGate.from_file(gate_path)        # or RegimeGate()  == OFF == all 1.0
# A's selector, per (edge, bar):
g = rg.gate(edge_name, regime_meta)         # ∈[0,1]; 1.0 when OFF/unseen
conjunctive_score = s_tech * g_fund * g     # A composes; E does not fork A
```
`regime_meta` is the existing per-bar dict (already carries the causal
`hmm_regime`). E delivers the label + the gate; A consumes; the gate reads
the HMM, not the advisory.

## The measurement — scope + why the gate ships OFF now
Populating the gates needs **per-(edge, HMM-regime) performance** — each
edge's contribution attributed by the causal HMM regime. That attribution
requires a backtest run (the alpha engine + the `regime_tracker` fed the HMM
label): there is **no re-labelable per-edge trade data on disk** (the
aggregated `regime_edge_performance.json` is empty; trade logs carry no
per-edge+date+pnl). Per the task, that full per-edge-per-HMM-regime
attribution + the OOS portfolio measurement (vs the robo) is the **director's
composition step** (A's selector + C's robo gate). My module derives the
label and builds the gates from those stats when they exist; **until then it
correctly ships default-OFF.**

## Honest read — does the HMM gate clear where the advisory failed?
Two things must hold for the gate to be a real lever; the evidence splits:
1. **A validated regime LABEL — CLEARS.** The advisory gate failed largely
   because its 5-axis `regime_summary` is a heuristic, not a validated
   predictor (T-172: the production advisory regime was effectively blind
   pre-crisis). The HMM `p_crisis` is the validated one (AUC 0.887, causal,
   generalizes to fast/credit crises — T-172). So the *input* is strictly
   better than what failed.
2. **Differentiated + STABLE per-edge-per-regime structure — LOW PRIOR.** A
   good label only helps if edges genuinely perform differently across
   regimes AND that difference is stable out-of-sample. The accumulated
   evidence is pessimistic: T-196 (Foundry H0 — no alpha in the book), T-180
   (base borderline; 12/13 edges factor-negative; 3 flow edges carry ~94% of
   PnL), and the retail conditional-selection prior (<20%, T-208). On a book
   with little unconditional edge, regime-conditioning is unlikely to
   manufacture *stable exploitable* structure — it more likely reproduces the
   same noise that net-negatived the advisory, just on a cleaner axis.

**Honest prior: the gate likely stays correctly OFF (a clean null)** — which
the pre-registration named as a valid outcome. The definitive answer is the
composition-time per-edge-per-HMM-regime backtest; the value delivered now is
that the gate is **built, validated-label, contracted, tested, and default-OFF**,
so that measurement is a single pre-registered structure on a ready input —
and if it nulls, the gate stays OFF and the regime half of the central thesis
closes cleanly on evidence.

## Constraints honored
- Engine E lane; repoints the existing `regime_gate` hook + the validated HMM
  (no new engine, no boundary cross, no live-sizing wire — it feeds A's
  gated selector).
- Pre-registered (1 structure, no sweep); N_trials += 1; OFF-default;
  canon literally unchanged; honest about the likely null.
- NOT wired into live sizing; the OOS robo measurement is the director's
  composition step.
