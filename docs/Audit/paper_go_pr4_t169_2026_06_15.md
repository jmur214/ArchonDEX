---
task_id: T-2026-06-15-169
title: PAPER GO — PR-4 (paper-scope boundary) + start the sustained paper run + deep-window safe_f
date: 2026-06-15
substrate: n/a (archival + boundary doc + live paper Day-1 + safe_f on the longest available equity curve)
scope: Archive/ (3 dead stubs), one deprecation in mode_controller (a never-constructed adapter), docs (boundary + scorecard + health_check); paper_trader/ unchanged. NO live-money path.
outcome: **PR-4 done (paper-scope).** The dead live stub (`live_trader/` + `storage/state_manager.py` + `brokers/alpaca_broker.py`) is archived; the deployment boundary now reads PAPER-ALLOWED / LIVE-HARD-GATED on (a) paper-validation AND (b) beating the Schwab robo net-of-costs/after-tax. The paper run is started: an armed `mean_variance` day ran end-to-end on the LIVE paper account (3/3 reconcile clean, account flat) and surfaced a real Day-1 finding — **Alpaca's OPG submission window is 7:00pm–9:28am ET** (code 40310000), sharpening the T-146 one-pager. Deep-window safe_f: the canonical 26yr re-anchor (`158fe678`, −33% MDD) isn't downloaded locally; the interim **12yr crisis-light number is 1.104** (already far below the benign-2024 1.602) — the −33%-MDD deep-window number will bind **< 1** (the live-size cap `min(1, safe_f)`); requested the 26yr dir.
---

# T-169 — PAPER GO

## PR-4 (paper-scope) — archive the dead stub + move the boundary

**Archived** to `Archive/pr4_dead_live_stub_t169/` (git-tracked rename,
never deleted — README in the dir):
- `live_trader/` (broker_interface + live_controller) — gtc-market
  stub, dead Path B sizing, never routed exits.
- `storage/state_manager.py` — single-blob state; only the archived
  live_controller imported it.
- `brokers/alpaca_broker.py` — read `ALPACA_API_SECRET` (absent from
  `.env`) → never authenticated; health_check MEDIUM, now RESOLVED.

**The one code edit** (necessary to archive `alpaca_broker.py` without
breaking the live backtest path): `mode_controller`'s
`AlpacaExecutionAdapter` was the only consumer of `AlpacaBroker`. It was
**never constructed anywhere** and FABRICATED fills at intended prices
(the T-159 anti-pattern). It is now deprecated (raises
`NotImplementedError` pointing to `paper_trader/`) with the
`AlpacaBroker` import removed from module scope. The active path uses
`DryRunExecutionAdapter` — untouched. Full collection clean (2552
tests); 184 paper/contract/controller tests green.

**Boundary** (`docs/State/deployment_boundary.md`, new load-bearing
header): **PAPER trading ALLOWED** (no real money); **LIVE money
HARD-GATED** on BOTH (a) the machine paper-validated per the §5
promotion criteria AND (b) a strategy beating the Schwab robo benchmark
net-of-costs/after-tax (user bar, 2026-06-15). No real-money path
enabled; live sizing (when it ever opens) capped at `min(1,
safe_f_deepwindow)`.

## Paper run — Day 1 (live paper account)

The armed loop ran end-to-end with the **designated `mean_variance`**
allocator (interlock fired: runtime == designated), dyn-opt ON,
moo_moc, Roth/$5K. **3/3 reconcile cycles clean** vs real broker truth;
**account left FLAT**. Scorecard: `docs/State/paper_run_scorecard.md`.

**FINDING (live-vs-design, Day 1, zero risk): the OPG submission window
is 7:00pm–9:28am ET.** Submitting outside it returns `APIError code
40310000, status 403: "opg orders must be submitted after 7:00pm and
before 9:28am"`. The synchronous driver ran the clock at 17:38 ET
(before 7pm) → the OPG submit was rejected; the M1 per-order guard
caught it, journaled a schema-complete `submit_error`, and the day
completed clean (the fix2/fix3 hardening working exactly as designed on
a real broker error). **This is a scheduling constraint, not a reject
of our orders** — the real daily cadence fires `submit_opg` at ~09:00
ET (inside the window). It sharpens the T-146 live one-pager (window =
7pm→9:28am, not just "before 9:28am") and motivates a one-line
scheduler pre-check (gate `submit_opg` on the window). No real fill yet
(market closed + the window); slippage-vs-T-146 telemetry begins the
first time `submit_opg` fires inside the window at a market open.

**Promotion-criteria status:** Day 1/60. Reconcile 100% clean, 0
drifts, 0 kill-rule violations; slippage/fill telemetry pending real
fills. Kill ACTIONS stay SHADOW for the first stretch (observe).

## Deep-window safe_f

The eventual live-size gate is `min(1, safe_f_deepwindow)` — never > 1.
The canonical input is the **26yr re-anchor** (`158fe678`: Sharpe
0.751, ci_low 0.382, CAGR 7.3%, **MDD −33%**), a CLOUD run **not
downloaded** to either local worktree (searched both repos). The
longest local equity curve is a **12yr crisis-light window** (2014–2025,
MDD only −16% — excludes dotcom+GFC).

| window | MDD | safe_f | CAR25 @ safe_f | P(DD>20%)@f1 | mdd95@f1 |
|---|---:|---:|---:|---:|---:|
| 2024 (benign single year, T-151) | small | 1.602 | 3.6% | 0.000 | 12.85% |
| **12yr crisis-light (interim)** | −16% | **1.104** | 6.66% | 0.023 | 18.23% |
| 26yr re-anchor (the GATE — pending download) | **−33%** | **expected < 1** | — | — | — |

The interim 12yr safe_f (1.104) is already far below the benign-2024
1.602 and sits right at the cap — its mdd95@f1 is 18.2% (near the 20%
tolerance). On the −33%-MDD 26yr record (with dotcom+GFC, where 2-year
windows routinely breach 20%), safe_f will bind **below 1**, so the
live-size cap `min(1, safe_f)` will be the deep-window safe_f, < 1 —
exactly as the dispatch expected. **Requested:** download the
`158fe678` 26yr run dir (A/B have it) to compute the canonical number;
it's one `compute_safef_car25` call once the equity curve is local.

## Files

- `Archive/pr4_dead_live_stub_t169/` — the 3 stubs + README
- `orchestration/mode_controller.py` — AlpacaExecutionAdapter deprecated, import lazified-away
- `docs/State/deployment_boundary.md` — current paper-allowed/live-hard-gated header
- `docs/State/paper_run_scorecard.md` — NEW; the §5 scorecard (Day 1)
- `docs/State/health_check.md` — the live-stub MEDIUM marked RESOLVED
- this audit

## NOT done / followups

- The canonical 26yr deep-window safe_f (needs `158fe678` downloaded).
- A scheduler pre-check gating `submit_opg` on the 7pm–9:28am window
  (one-line followup; today's reject was caught + handled cleanly).
- The sustained 60-day cadence (operational; this starts Day 1 + the
  scorecard). Engine-constructed order flow (PaperOrderConstructor) vs
  the driver's single demo order — the construction is wired/tested;
  driving it daily is the cadence's job.
- NO live-money path (hard-gated; boundary is paper-scope only).
