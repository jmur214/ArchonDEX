# Whole-Project Gap Audit — "What Are We Missing?" (2026-07-02)

**What this is:** an 11-agent adversarial audit (4 repo-forensics + 5 frontier-research + skeptic + completeness-critic, ~1.1M tokens) commissioned by the user's directive: *"the hunt is not over — find what we're missing, what was killed for one reason that could serve another, where the system isn't thinking like a trader."* Every load-bearing claim below survived an adversarial skeptic pass with code/data/URL verification. This document supersedes narrower claims in the two prior fresh-eyes briefs where noted.

---

## PART 1 — THE HEADLINE: our own gauntlets are biased AGAINST our own sleeve (verified line-by-line)

The "trend sleeve loses ~1%/yr to the robo" framing — the basis of "it's only a defensive trade" — is substantially a **measurement artifact**:

1. **The sleeve's flat leg earns 0%** (`core/trend_overlay.py:84-85,105`) while the sleeve averages ~35% in cash. At the actual DGS3MO path (avg ~1.9%, 2000-2025) that understates the sleeve **~0.65%/yr**. Damning: the fix was KNOWN — `carry_sleeve_gauntlet_t247.py:213-217` explicitly credits carry's flat leg the short rate ("NOT a 0% drag") — and was never back-ported to the sleeve. No offsetting timing effect (flat bars coincide with HIGHER-rate regimes).
2. **The robo's cash is credited a flat 4%** (`trend_sleeve_gauntlet_t236.py:9,37`) vs the ~1.9% historical average → overstates schwab_like **~0.43%/yr** — before even modeling the real robo's below-market sweep (the $187M flaw). T-236 also charges the robo no expense ratios and free rebalancing. The four gauntlets disagree with each other on cash conventions (T-248's robo gets 0%).
3. **Net bias: ~0.65%/yr pro-robo vs 60_40; ~1.1%/yr vs schwab_like.** Honest expectation for a fair re-run: gap vs 60_40 ≈ 0.1-0.35%/yr; vs schwab_like the sleeve **plausibly wins wealth outright** (unproven until the re-run, which must also charge the sleeve's own TC).
4. **The deploy-gate scorecard's robo uses PRICE-ONLY AGG** (`combined_candidate_scorecard.py:198`; measured AGG_1d CAGR −0.17%/yr vs TR ~+3.3%) — the robo bar is missing **~1.3%/yr**. Past H0s (T-215) are therefore ROBUST/conservative, but every FUTURE `evaluate_deploy_readiness` pass faces a too-easy bar — **a latent false-DEPLOY generator. Most operationally urgent fix.**
5. **The flagship T-236 number is NOT REPRODUCIBLE** — `/tmp/t236/` (bond_synth.csv, gold_gcf.csv) is deleted; no committed generator exists. `[NN-CENSUS]`-class defect on the single most-quoted number in CURRENT_STATE (re-derivable from the audit doc's formula, but re-derivation ≠ reproduction). Also its MBL gate used point-Sharpe, not ci_low.
6. Stooq-substrate gauntlets (T-234/248/251/253) are only PARTIALLY dividend-adjusted (Stooq AGG +1.0%/yr vs TR ~3.0%) — that bias runs pro-SLEEVE and roughly cancels the cash bias vs 60_40 on those scripts. The T-215 equity book was never credited dividends (~1-1.8%/yr) — H0 margin overstated but verdict unchanged.

**GOVERNANCE (critic hole 10):** the fair, reproducible T-236 re-run (committed bond_synth generator; DGS3MO cash BOTH sides + a below-market robo-sweep variant; ER+TC both sides; paired difference-CIs; census-archived inputs) is a **named BLOCKING gate before the paper schedule is enabled**. The Monday armed run (execution-fidelity) may proceed; the enable decision waits.

## PART 2 — the kill-ledger vindication: ~12 kills preclude only STANDALONE use, and the "think like a trader" machinery exists but was never fed

- **Structural finding (T-122/T-156/T-158, three audits converge):** `policy.py` inverse-vol normalization **algebraically cancels any uniform timing signal** (weight diff exactly 0.0). Every macro/seasonality/timing edge "killed" via Gate-1 +0.000 inside the equity ensemble (macro_yield_curve, credit_spread, dollar_regime, seasonality, VRP-timing) was killed by the ARCHITECTURE, not evidence. They have **no valid negative verdict as sleeve-level sizing/conditioning inputs.** (T-233 did give credit/VIX a real kill — but only as overlay FRONT-RUNNERS.)
- **The conditional shelf** (`docs/State/conditional_shelf.md`, built T-166 for exactly this purpose) **stalled after 5 entries (2026-06-15)**; the T-233→T-254 kill-wave added zero entries despite shelf-shaped verdicts (T-250 even-week "real but standalone-marginal"; T-253 DBMF "narrow sustained-bear hedge"). Backfill + re-arm the ADD-at-burial convention.
- **Top reusable ingredients (skeptic-ranked):** #1 FOMC even-week × sleeve conditioning (T-250's own queued step-2, never run). #2 T-252 conditional vol-targeting — built, validated (MaxDD −55→−47%), **never composed into the deploying sleeve**. Then: credit/VIX-term as confirmation/off-leg-selection (never tested in those roles); Wide-9 as a convex-satellite (killed only as core); DBMF as a narrow sustained-bear satellite (skeptic: the HMM-gated version is blocked — the HMM is weakest exactly in slow bears, shelf entry #5's known blocker); overnight-split-as-feature + the T-157 auction-cost condition (≤0.5bp/side re-opens LPS; the live paper machine can measure it); PEAD × insider interaction (both parts killed individually; the interaction is documented and untested — the built T-216 selector's first real payload).
- **18 scoped-but-never-run items** catalogued (g_regime never populated; conjunctive 3-way verdict never run; positioning parquets with zero consumers; T-230 no-borrow fix held; Databento options spike; even-week × sleeve; GDELT/13D-G/USASpending from inventory #1...).
- **Honest counterweights (kill-ledger agent's own):** the core H0s are NOT premature (don't re-litigate single-gene/ensemble/concentration/BAB/13F); quad-witching ≈ triple_witching already failed T-196; VIX-MR's tradeable equity form = the refuted T-174 overlay; Wide-9 vol-weighting WAS tested (kills the skew); "smarter allocator above strategies" has failed 3 direct internal tests + Allocate Smartly's 50-yr external test.

## PART 3 — the data unlocks (most claimed "walls" were on-disk artifacts)

1. **The carry "DATA-BLOCK" is an on-disk artifact.** A 1.7GB Stooq bundle ALREADY ON DISK (`data/raw/stooq/daily/us/`) has GLD/TLT/IEF/EEM/EFA/AGG/VNQ/LQD from 2005, DBC 2006, UUP/HYG 2007, QQQ 1999 — all through 2026-05. The processed caches were truncated at 2020-04-09. Proven ingest+merge+splice tooling exists (`ingest_stooq_us_daily.py`, T-082b, T-167 yfinance-splice with 1.000000 basis). 21-24yr windows drop the multi-asset-carry MBL bar from ~1.36 (unclearable) to ~0.68-0.73. **MUST bundle TR reconciliation** (Stooq is split-only) or we import the dividend bias while fixing the window. Bond-carry H0 does NOT reopen (measured on 2003+ AGG).
2. **Free vol-complex verified on CBOE CDN:** SKEW 1990+, VVIX 2006+, VIX6M 2008+, VIX3M 2009+ (VXV archive ~2007), VIX9D 2011+. VVIX was NEVER ingested (the foundry uses a self-documented lagging realized-vol proxy); VIX parquets stale (end 2025-04) by fetch flag. Equity yield (Shiller ie_data, 1871+) unreferenced — completes the carry equity leg.
3. **On-disk, zero consumers:** NAAIM (~20yr weekly), EPU/GPR daily (1985+), FINRA short interest/volume, SEC FTD, 641 insider parquets, minute-features (or_frac/last30_ret — **already PASSED pre-registered MI screens as regime features**, dispatch recommendation never executed), sector SPDRs, VIXY/SVXY/DBMF/KMLM in Stooq raw.
4. **Alpaca serves DELISTED small/micro-caps** (empirically probed: 16/16 full histories 2016→delisting; `/v2/assets?status=inactive` returns a 19,243-name delisted list). **A $0 survivorship-complete small-cap panel 2016-2026 is buildable NOW** (+ EDGAR Form 25/XBRL for events). Pre-2016 free recovery ≈ 0%.
5. **CBOE strategy benchmark indices are FREE, daily, full-cycle** (verified live + Wayback master xls 1986-2019 spliced): PUT/BXM/BXMD/CLLZ/CMBO/PPUT 1986+. The "options = paid data" exclusion was factually wrong at the strategy-index level. (Mirror the Wayback xls — load-bearing.)
6. **Preservation risk:** 2.6GB of EDGAR corpora (Lazy-Prices 10-K/Q, 8-K, 13F, insider) live ONLY in agent worktrees — copy to main data/ or S3 before any worktree cleanup.
7. Stale in-code DATA-GAP docstrings (hyg_lqd_spread, faber, vix_change_5d) claim data is missing that now exists — fix per the docs-for-AI directive.

## PART 4 — the frontier verdicts (skeptic-tempered)

- **Norgate: the user was RIGHT; the pass-2 "$148.50 falsifier" claim was WRONG.** Silver is survivor-ONLY. True survivorship-free = **Platinum $346.50/6mo** ($630/yr) — delisted back to 1990 (~25k securities) + **PIT Russell 2000/S&P 600/400 membership** (the exact enabler for low-coverage PEAD) + formerly-listed OTC (post-delisting decay). Sequencing: **$0 Alpaca+EDGAR pilot first; Platinum only to EXTEND a live signal** (it also halves the DSR bar: 36yr window → SR ~0.49 vs ~0.91 on 10yr). Alternatives: Sharadar (delisted-inclusive PIT fundamentals, price login-gated), EODHD $199/yr (weakest completeness), FirstRate one-time (no PIT membership).
- **Put-write (CBOE PUT index): data-exclusion refuted, ROLE-kill survives.** Computed full-cycle: PUT CAGR 9.6%, Sortino 1.03, MaxDD −37%, skew −2.94; the entire risk-adjusted edge vs SPX-TR is pre-2013; COVID = 85% of the crash then missed the V-recovery. Pairing with trend is HALF-right (rescues slow bears: 2008 50/50 −12% vs PUT −26%; nothing rescues gaps — corr 0.613, trend −3.9% in PUT's worst months). Implementation: XSP CSPs need ~$65-70K/contract (INFEASIBLE at $5-15K; the pass-2 $5-6K figure was 10× off); the only wrapper (WTPI, ex-PUTW) tracks a DIFFERENT index since 2022 → `[NN-SUBSTRATE-REVERIFY]` transfer risk. ONE pre-registered test as the income 3rd sleeve (50/50 sleeve+PUT vs both robos, wrapper-transfer check mandatory; skeptic: expect the T-251 points-win/CI-fail base rate ~60-65%). **Income-leg screener first** (critic hole 9): PUT vs BXMD vs JEPI-proxy vs preferreds vs muni-CEF composite at $0 before committing the slot.
- **CEF discount capture — the one genuinely NEW retail-sized alpha class surfaced:** long-quintile significant in BOTH OOS halves 1998-2011, no decay (Patro-Piccotti-Wu), ~10%/yr over market, costs insufficient to kill, capacity-constrained (institutions excluded — the classic retail edge), live corroboration (Saba CEFS 12.5% net since 2017). Free data via CEFConnect **needs a PIT/survivorship audit FIRST** (stale-sample + leverage/activism confounds; prior medium).
- **CTA-practice upgrades:** the sleeve is one generation below standard, but most upgrades are SHAPE not RETURN in an unlevered Roth. The ONE genuine return lever: **the off-leg** (defensive-asset momentum among {BIL, IEF, (GLD)} when flat — subsumes the corr-conditional idea; Antonacci's off-leg carry ~210bps historically; skeptic: don't quote 0.3-0.8%/yr as EV — bond-bull-era-dependent, 2022 must-not-degrade window at genuine risk, knowingly duration beta). **Multi-speed ensemble** {~3,5,8-12}mo fractional votes = spec-luck insurance (ReSolve 1,226 variants: single-spec edge is luck; ensemble cut MaxDD 17.4→13.2%; adjacent lookbacks differ 2,151bps/yr) — minimal effective-N (robustness aggregation). **Tranching** the rebalance = 2/3 dispersion cut, free. Vol-weighting = TRAP (shifts 55-60% into AGG, widens the wealth gap — internally confirmed twice). Continuous forecasts ≈ 0-10% at our granularity (turnover reduction only). **Jump models** beat HMM with real OOS discipline (S&P Sharpe 0.68 vs 0.54, arXiv 2402.05272, OSS implementation) — swap into T-252's consumer, do NOT re-open regime-gating (T-220 stands). Canary assets = just fast trend (defensive 60% of the time); fold into multi-speed.
- **Intraday: the 78-390× exclusion was over-broad** — Alpaca free tier serves full-SIP historical minute bars to ~2016 (single instrument = trivial compute). But ORB's 675% headline = 4x leverage + 49% shorts + zero slippage (extracted from the paper; the authors' own firm found >3× result dispersion across data providers). Honest unlevered long-only ORB ≈ zero net. Better single probe: **Gao-Han-Li-Zhou market intraday momentum** (JFE 2018, long-leg, executable at the tighter 15:30 spread). No-PDT confirmed (cash account), but T+1/GFV caps capital ~50% for daily round trips. ONE probe slot, prior 15-25%, "close the question honestly."
- **Conditioning-input ranking (evidence-based):** (1) **breadth %>200dma from our OWN PIT panel** (zero data risk, full 26yr history, BreadthDetector exists); (2) VIX/VIX3M term-structure state (free 2007+); (3) VVIX extreme-tail de-risk (2006+); (4) put/call (2019 continuity break), AAII extremes. **Skip:** NAAIM (self-declared non-predictive), COT on S&P (academically null + 3-day lag), raw SKEW (noisy). Design constraint: **continuous 0.5-1.5× sizing tilts, NEVER gates** (T-220/T-233/T-221), scored vs TWO nulls: unconditioned sleeve AND T-252 realized-vol targeting.
- **TAA products: REJECT** relative-strength selection (GEM post-pub 5.9%/yr vs 17.4% backtest; ADM adopters −15%/yr from Jan-2022), momentum-of-strategies (Allocate Smartly 50-yr test: strictly suboptimal), sector rotation. Bonus structural edge confirmed: TAA's biggest real cost is short-term-gains tax (HAA 54% ST) — **zero in the Roth**; published "fails net-of-tax" verdicts don't transfer to us.

## PART 5 — the critic's remaining holes (all accepted)

(1) Anchor the robo proxy to the REAL Schwab Intelligent Portfolios published net returns (Backend Benchmarking Robo Report 2016-2025) — the proxy has only ever been calibrated against itself; the scorecard's own comment admits the real product is ~12 asset classes. (2) **Audit the Sortino/MaxDD CI machinery itself** — block-bootstrap MaxDD is structurally biased (resampling shortens drawdowns); run a one-day coverage simulation before more ci_low-gated verdicts. (3) **The paper validation can only validate EXECUTION, not performance** (a monthly-signal sleeve ≈ no independent obs in 6-12mo): pre-register execution-fidelity gates (tracking error, slippage vs assumed, order-state errors, N clean days) and say so in the go-live doc. (4) **Integer-share discretization unmodeled**: Schwab has no fractional ETF shares; 1 SPY ≈ 12% of a $5K account; simulate integer shares at $5K/$10K/$15K (dynamic_optimizer T-139 exists, never run in a sleeve gauntlet); consider SPLG/GLDM share classes. (5) **Test-budget governance**: the agents proposed ~20 trials with overlaps — dedupe into ONE ranked pre-registration queue with per-family effective-N (off-leg proposed 2×, multi-speed 3×, vol-term 2× → one arm each). (6) Index add/DELETION event family missed — S&P deletion-reversal is documented, long-only, retail-sized, and the PIT membership panel is on disk. (7) Treasury/bond month-end + auction calendar — fold ONE arm into the off-leg test. (8) BTC as a 4th sleeve asset (IBIT Roth-holdable; never examined; one exploratory arm, short-window caveat). (9) Income-leg screener before committing the 3rd-sleeve slot (above). (10) The blocking-gate wire (Part 1).

## PART 6 — THE PROGRAM (deduped, sequenced, test-budget-governed)

**WAVE 0 — fix the yardstick (BLOCKING, zero new N_trials — corrections, not hypotheses):**
0.1 Reproducible + FAIR T-236 re-run (committed bond_synth generator; DGS3MO cash both sides + below-market robo-sweep variant; ER+TC both sides; paired ΔCIs; archived inputs). **Gates the paper schedule enable.** Prediction: sleeve ≈ ties 60_40 on wealth, likely beats schwab_like — while keeping the 2-3× drawdown win.
0.2 Fix the scorecard robo (TR AGG + historical short-rate cash) — kills the false-DEPLOY generator.
0.3 Stooq ETF ingest + TR reconciliation + VIX/VVIX/SKEW/Shiller/CBOE-index fetches (one data task; unlocks carry re-test, vol-complex, put-write substrate).
0.4 Integer-share simulation at $5K/$10K/$15K; CI-coverage simulation; real-robo anchoring.
0.5 Process: preserve EDGAR corpora; backfill the conditional shelf (T-250/T-253/T-214/T-247/T-233); fix stale DATA-GAP docstrings; mirror the Wayback CBOE xls.

**WAVE 1 — upgrade the working sleeve (pre-registered, ONE arm each):**
1.1 Off-leg: momentum-gated defensive asset {BIL/IEF/(GLD), else cash} when flat; 2022 = named must-not-degrade window; + one bond-calendar sub-arm (near-zero marginal N). THE return lever.
1.2 Multi-speed fractional ensemble {3,5,8,12}mo (merges 3 proposals; robustness aggregation, minimal effective-N) + lookback-dispersion scan (0 hypotheses) + tranching measurement.
1.3 Compose T-252 conditional vol-targeting into the sleeve (built + validated already).
1.4 Even-week × sleeve conditioning (T-250's queued step-2).

**WAVE 2 — the return-half candidates (new sleeves for the waiting T-248 composer):**
2.1 Income-leg screener ($0) → ONE pre-registered income-sleeve gauntlet (likely PUT-index 50/50 w/ wrapper-transfer check).
2.2 CEF discount: data audit → long-quintile sleeve test (the genuinely new alpha class).
2.3 Multi-asset carry re-test on the unlocked 21-24yr substrate.
2.4 Small-cap PEAD-low-coverage: $0 Alpaca+EDGAR 2016-2026 panel (event-study design) → Norgate Platinum $346.50 ONLY if the pilot shows signal.

**WAVE 3 — conditioning library + honest closures (strictly gated, continuous tilts only):**
3.1 Breadth-from-own-panel sizing tilt; 3.2 VIX-term/VVIX tilt (vs T-252 as the null); 3.3 jump-model swap for T-252's consumer; 3.4 index-deletion event study (data on disk); 3.5 ONE intraday probe (Gao long-leg primary / ORB-1x secondary — closes ORB/VWAP honestly); 3.6 BTC 4th-asset exploratory arm.

**Honest overall calibration:** Wave 0 is pure correction and pays immediately (the sleeve's true standing improves). Wave 1's off-leg + composition is the most defensible ~0.3-0.8%/yr (duration beta, honestly labeled). Waves 2-3 carry the project's own base rate — most arms will confirm H0 or points-win/CI-fail; the CEF sleeve and the PEAD pilot are the two with genuine new-alpha character. The meta-lesson stands: expect deflation under the honest apparatus — but for the first time since the inflection, the honest apparatus itself was found to be deflating US, and fixing that is not hope, it's arithmetic.
