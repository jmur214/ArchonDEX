diff --git a/.github/workflows/build_backtest_image.yml b/.github/workflows/build_backtest_image.yml
index dcc4f10..39cb0f2 100644
--- a/.github/workflows/build_backtest_image.yml
+++ b/.github/workflows/build_backtest_image.yml
@@ -60,6 +60,26 @@ jobs:
       - name: Checkout
         uses: actions/checkout@v4
 
+      # T-2026-06-05-109 fail-loud precheck. The OIDC action below silently
+      # reports "Credentials could not be loaded" when AWS_ROLE_TO_ASSUME
+      # is missing, which T-109 traced to ~10 consecutive failed runs
+      # since 2026-05-24 (the secret has never been configured for this
+      # repo). Fail with a NAMED reason so the next maintainer doesn't
+      # have to dig through the OIDC action output.
+      # See docs/Audit/ecr_rebuild_static20_t109_2026_06_05.md for the
+      # one-time IAM + secret setup the user must do.
+      - name: Pre-check required secrets
+        run: |
+          if [ -z "${{ secrets.AWS_ROLE_TO_ASSUME }}" ]; then
+            echo "::error::Repo secret AWS_ROLE_TO_ASSUME is MISSING."
+            echo "::error::This workflow has been failing on every main push since 2026-05-24."
+            echo "::error::Fix: GitHub repo → Settings → Secrets and variables → Actions →"
+            echo "::error::      add secret AWS_ROLE_TO_ASSUME with the IAM role ARN."
+            echo "::error::See docs/Audit/ecr_rebuild_static20_t109_2026_06_05.md for the"
+            echo "::error::IAM role + OIDC trust-policy configuration (one-time setup)."
+            exit 1
+          fi
+
       - name: Configure AWS credentials via OIDC
         uses: aws-actions/configure-aws-credentials@v4
         with:
diff --git a/docs/State/TASK_LEDGER.md b/docs/State/TASK_LEDGER.md
index 5bb32f0..a692e9e 100644
--- a/docs/State/TASK_LEDGER.md
+++ b/docs/State/TASK_LEDGER.md
@@ -60,6 +60,7 @@ Backfilled from T-035 (cloud era start, 2026-05-12) forward. Use `git log` and `
 | T-108 | 2026-06-05 | Managed-futures trend sleeve Phase 0 diagnostic (Agent A; Path B Layer 2) | done | — | — | **VERDICT: RECONSIDER (DBMF/KMLM ETF route)**. 8-ETF diversified-futures basket (SPY/TLT/GLD/USO/UUP/EEM/IEF/DBC) on Stooq 2008-2025 (17.4yr, UUP-bound floor 2007-02-20). **SKEW -0.408 ci [-0.754, -0.039] FAILS the make-or-break vs equity-trend -0.133** — actually WORSE than the falsified equity-trend on the structural property. **BUT crisis-alpha is REAL**: 8/8 crisis windows beat SPY (2008 +28.6pp, 2022 +35.7pp, COVID +11.0pp, all 5 others +3-9pp). Sharpe +0.505 ci_low +0.085, Sortino +0.471 ci_low +0.078, SPY corr 0.29, MDD -26% (vs SPY -52%). Self-built spot-ETF trend captures DIVERSIFICATION + CRISIS-ALPHA but cannot deliver POSITIVE-SKEW CONVEXITY — likely structural (spot ETFs lack futures carry/roll/leverage). DBMF/KMLM (real managed-futures ETFs) is the natural next test. NO Phase 1 integration; NO engine code touched. | `managed_futures_trend_t108_2026_06_05.md` |
 | T-110 | 2026-06-05 | DBMF + KMLM managed-futures ETFs Phase 0 (Agent A; T-108 RECONSIDER follow-up) | done | — | — | **VERDICT: MIXED — crisis-alpha diversifier, NOT skew cure.** Futures-wrapper hypothesis from T-108 REFUTED: DBMF skew -0.749 ci [-1.026, -0.425] and KMLM skew -0.849 ci [-1.302, -0.424] — BOTH worse than T-108 spot basket (-0.408) and equity-trend (-0.133). **BUT crisis-alpha is MORE extreme**: DBMF 2022 +57.2pp vs SPY, KMLM 2022 **+73.3pp**, DBMF COVID +11.2pp. **KMLM SPY corr -0.139, beta -0.12** (first anti-correlated product in any Path-B test). Sharpe DBMF +0.517 ci [-0.175, +1.188] (7yr too short to clear strict gate), KMLM +0.409 ci [-0.346, +1.249] (5.4yr too short). MDD DBMF -23.7%, KMLM -28.1%. **Path-B Layer 2 'structural skew cure' thesis CLOSED-OUT NEGATIVE across 3 product types (T-007, T-108, T-110)**; Phase 1 should retarget "MDD reduction at non-worse Sharpe" Pareto KPI. NO integration; NO engine code touched. | `dbmf_kmlm_managed_futures_t110_2026_06_05.md` |
 | T-111 | 2026-06-05 | Engine B defensive-wiring systemic sweep + lift-out design + drawdown PoC (Agent B, propose-first) | done | — | — | **7-of-10 Engine B defensive controls are DEAD on Path A.** LIVE on Path A: advisory `suggested_max_positions` floor + `suggested_exposure_cap` + sector-cap consumer (producer dead per T-104). DEAD on Path A: drawdown kill-switch (T-106), HMM-modulated `risk_scalar` (T-101), `risk_per_trade_pct` (T-088), 6 other `risk_scaler` sub-multipliers + Path-B `max_pos_value_pct` cap + ATR stop-widening. T-111 PoC ships pre-path lift of the drawdown kill-switch behind new flag `drawdown_kill_switch_apply_on_path_a` (default OFF). Canon-md5 A/B: OFF `0145c03a6496…` ≡ T-101 baseline (bitwise-identical), ON `52202e510d27…` (DIFFERS). Determinism --runs 3 PASS on default OFF. capability_ledger updated. HMM-`risk_scalar` Path-A lift is a separate propose-first per dispatch no-blind-revive constraint. | `engine_b_defensive_wiring_sweep_t111_2026_06_05.md` |
+| T-109 | 2026-06-06 | Stale ECR root-cause + image refresh + static-20 cloud A/B (Agent B) | done | 4 | 3 | **REJECT static-20** (Sharpe −0.255 on 16-yr / −0.091 on 26-yr; MDD improves but Sharpe ci_low criterion fails both windows; the 5-yr local +0.043 from T-107 was 2024-luck). **Part A:** CI workflow failing since 2026-05-24 because `AWS_ROLE_TO_ASSUME` repo secret never configured; fail-loud precheck added + IAM-role+OIDC+secret config documented for user (propose-first). **Part B:** image refreshed `2026-06-06T01:30:24` (was 9-day-stale); det --runs 3 PARTIAL (2/3 identical, 1/3 drift, Sharpe-identical 1.6) — T-099 floor partial on Py3.14-slim base, flagged. **Surprise:** arm0_baseline_26yr Sharpe 0.446 vs T-092 published 0.246 — fresh image produces materially better baseline; T-092 numbers need re-baseline. arm1_26yr cell timed out at 4h during S3 upload AFTER full backtest completion; metrics recovered from CloudWatch logs (6,538 snapshots). | `ecr_rebuild_static20_t109_2026_06_05.md` |
 
 ## Closed-pre-T-035 (digest only)
 
