---
name: engine-b-doc-buried-defensive-capabilities
description: Engine B (Risk) ships ≥7 crisis/de-gross/tax/factor capabilities that the registries (capability_ledger + DESIGN_FIDELITY) do not surface or list as dead — the "refuted/branch-held-but-present" doc-gap pattern
metadata:
  type: project
---

Engine B (Risk) carries multiple shipped behavior-altering capabilities that the registries (capability_ledger.md, DESIGN_FIDELITY.md) and living docs do not make discoverable or actively MIS-state. First found in the 2026-06-04 Path-B audit; EXPANDED 2026-06-22 in a post-merge-wave (T-204/211/212/216/218) registry-vs-code audit.

The buried / mis-stated capabilities (state as of 2026-06-22, all on main unless noted):
- **Crisis-floor on suggested_max_positions** — consumed ACTIVE at `risk_engine.py:861` (was `:729` in ledger; DRIFTED). `risk_advisory_enabled` defaults True + present in prod. HIGH Path-B, ACTIVE, in NO living doc, IS in ledger.
- **regime_transition_overlay (T-118)** — `regime_transition_overlay_enabled` (`risk_engine.py:188`, default False) → `RegimeTransitionOverlay` constructed `:286`, advanced `:392`/`:1041`, applied to Path-A `target_notional` at `:1080`. Fully wired, LIVE Path A, default-OFF. Tests `tests/test_regime_transition_overlay_t118.py`. **UNTRACKED in both registries.** The TRANSITION form of the de-gross lever (consumes combined HMM posterior).
- **Path-A advisory risk_scalar lift (T-116 PoC)** — `advisory_risk_scalar_apply_on_path_a` (`risk_engine.py:119`, default False), applied `:1080` via `_advisory_risk_scalar_mult`. Sibling of TRACKED T-111 drawdown Path-A lift; the T-116 lift is **UNTRACKED**.
- **WashSaleAvoidance + LTHoldPreference** — tax-aware order gates, wired in prepare_order (`:836` block-buy, `:724` defer-exit), config `portfolio_settings.json:38,43` (enabled:false). Behavior-altering (BLOCK/DEFER orders). **UNTRACKED.**
- **Regime-conditional vol-target multiplier** incl. `portfolio_vol_target_crisis_multiplier=0.40` (`risk_engine.py:144-148`; consumer `_compute_portfolio_vol_scalar` `:525`, applied `:940/1079`). GATED-OFF (needs `portfolio_vol_target_enabled` AND `_regime_aware`) AND refuted on 12-yr (T-055h). refuted-but-present, IS in ledger but `:251` line ref is STALE.
- **Drawdown-gated kill switch** — Path-B legacy consumer `:1188` (was `:940`), T-111 Path-A PoC `:826/884` lift. INERT default-OFF. In ledger.
- **FactorRiskModel** (`factor_analysis.py`) — ledger says "zero importers/orphaned" but T-209 added `decompose()` diagnostic (`:98-149`) imported by `tests/test_factor_risk_model.py`. SIZING still unwired (hook `:151-163` is explicit propose-first). Row is STALE on "zero importers".
- **correlation_regime sector-cap** — ledger says "DEAD CONSUMER (no producer)" but T-107 added GATED producer (`advisory.py:259-260`, flag `correlation_regime_in_advisory_enabled` default False, `regime_config.py:125`). Consumer `:876` reachable if flag flips. Row STALE.
- **T-218 factor-neutrality SIZING** (`factor_neutrality.py` + `factor_neutrality_enabled` + 6 caps) — BUILT on `feature/factor-neutrality-sizing-t218`, NOT on main (file absent on main, zero wiring in main risk_engine). **T-212 vol-target build** on `feature/voltarget-build-t212` (part-1 fail-loud floor branch-only; T-153's non-fail-loud floor IS on main). Both in TASK_LEDGER as `dispatched`/branch-held; NEITHER registry has a row → factor-neutrality could fall through the net like the conjunctive selector.

**Why:** Two burial modes here. (1) the negative VERDICT buries refuted-but-present code (vol-target/T-055h). (2) the MERGE WAVE + BRANCH-HELD work: capability_ledger/DESIGN_FIDELITY were not reconciled after T-204/211/212/216/218, so newly-wired (T-118 overlay, T-116 lift, wash/lt) and branch-built (T-218/T-212) capabilities never got rows. A `dispatched` TASK_LEDGER row is NOT a capability_ledger row — branch-held builds are invisible to the anti-build-bias registries unless explicitly added as DORMANT/branch-held.

**How to apply:** When auditing Engine B "what exists," do NOT trust charter/CURRENT_STATE/ledger alone. (a) Read RiskConfig dataclass fields + defaults directly + grep prod config (`risk_settings.prod.json` is MINIMAL — almost everything falls to dataclass default False). (b) grep risk_engine for every `self.cfg.<flag>` consumer to confirm Path-A reachability vs dead-Path-B (`else:` block after the `target_weight is not None` branch ~`:1054` = Path B = dead in prod). (c) check `git branch --contains <wire-commit>` for T-2xx work — TASK_LEDGER `dispatched` often means branch-only, NOT on main. (d) ledger line numbers DRIFT badly after any merge wave (Layer 3a WARN-class) — re-derive actual locations by grep, don't trust the anchor. See [[doc-gap-pattern-refuted-verdict-buries-shipped-capability]] and [[engine-e-regime-summary-repoint-blast-radius]] (risk_scalar shares E's risk_score source).
