---
name: engine-a-buried-defensive-paths
description: Engine A buried/untracked capabilities — risk_scalar brake (ACTIVE charter-violation), macro_yield_curve (code says active but edges.yml=RETIRED), conjunctive selector (NOW BUILT, DESIGN_FIDELITY stale), defensive_tilt screens (untracked, stale "not on prod path" docstring). Key lesson: read edges.yml not the auto-register status=.
metadata:
  type: project
---

Engine A contains crisis/regime de-gross capabilities that NONE of the living docs (CURRENT_STATE.md, engine_charters.md, high_level_engine_function.md) surface as A capabilities. Found during the 2026-06-04 buried-capability audit (motivated by the Engine B+E `portfolio_vol_target_crisis_multiplier=0.40` advisory.py path that was buried because T-055 was refuted).

**Why:** Same buried-capability mechanism as Engine B+E — defensive paths that got touched during refuted experiments (T-057 confidence gate, T-055 vol-target) had their negative VERDICTs recorded in MEMORY/CURRENT_STATE but the still-shipped CAPABILITY was never documented.

**How to apply:** When scoping T-092 Path B (HMM crisis kill-switch), these are pre-existing de-gross layers the kill-switch design must account for — otherwise crisis de-gross gets double/triple-applied.

Two HIGH/MEDIUM Path-B-relevant findings:

1. **`risk_scalar` crisis brake INSIDE A** — `signal_processor.py:543-551`. When `advisory.regime_summary in ("stressed","crisis")`, A multiplies every edge norm by `advisory.risk_scalar`. ACTIVE, default-ON. This is a CHARTER VIOLATION: docs attribute risk_scalar consumption only to Engine B (`high_level_engine_function.md:35`, Double-Counting Matrix `engine_charters.md:551` gives A a dash). Same fact applied in A (shrink forecast) AND B (shrink size) = the exact double-count the matrix WARNING exists to prevent. Charter line 88: "A should be opinionated about direction but NOT protective about risk."

2. **`macro_yield_curve_v1` edge — REFUTED THE "ACTIVE" ASSUMPTION (2026-06-22 re-audit).** Code auto-registers `status="active"` (`macro_yield_curve_edge.py:199`) BUT the LIVE registry `data/governor/edges.yml:1755` = `status: retired`. `EdgeRegistry.ensure()` WRITE-PROTECTS persisted status (comment at macro_yield_curve_edge.py:184-187), so the code default never wins. Net: it does NOT fire in prod — DORMANT/retired, not an active de-gross path. **LESSON: never trust an edge's in-code auto-register `status=` — always read `data/governor/edges.yml` for the live status (the write-protect makes them diverge).** Same trap on `low_vol_factor_v1`: code `status="active"` (line 140), live edges.yml:1778 = `paused`/`retire-eligible`. The capability_ledger's "ACTIVE -0.3 tilt" / "unknown — needs trace" row for macro_yield_curve is STALE-as-dead. Three siblings (credit_spread, unemployment_momentum, real_rate) also `retired`/`paused` in edges.yml.

Other A capabilities undocumented in living docs but lower Path-B relevance: confidence_gate (default-off, T-057 REFUTED), fill_share_capper (default-ON cap=0.25, anti-concentration), metalearner Layer-3 (default-off), per_ticker metalearner (default-off), paused_max_weight soft-pause ceiling (default-ON 0.5), SignalGate ML (gated on model file presence), MLPredictor RF gate (gated on data/models/rf_model.pkl presence), ALPHA_FORCE_SIGNALS + env threshold overrides, the DISABLED directional-regime-bias block (commented out).

**UPDATE 2026-06-22 — merge-wave (T-204/205/211/216/217) capabilities:**
- **Conjunctive selector NOW BUILT** (`signal_processor.py:510 _conjunctive_aggregate`, wired at :688, reachable via `EnsembleSettings.mode="conjunctive"`, default `weighted_mean`). DESIGN_FIDELITY.md:19 still says "NEVER-BUILT → BUILDING NOW (A/T-216)" with stale evidence (`signal_processor.py:615 weighted_sum ONLY; zero conjunctive in engines/`) — that's STALE/false now. The g_regime half is a real 3-way (calm/cautious/crisis × s_tech × g_fund), keyed on E/T-217's `hmm_regime_label` (`engines/engine_e_regime/regime_gate.py:63`) — the original bug (keyed on macro_regime vocab regime_summary never emits → g_regime≡1.0 → silent 2-way) is FIXED per the comment at signal_processor.py:489-500. Prod config has no `mode` key → correctly DORMANT.
- **defensive_tilt screens UNTRACKED + stale docstring.** `engines/engine_a_alpha/screens/defensive_tilt.py` (`quality_tilt_longs:112`, `high_ivol_exclusion:163`). Its own `__init__.py` + docstring (lines 18-23) claim "NOT imported by production path" — FALSE: `engines/engine_c_portfolio/phase1_composition.py:88-96` imports/calls both. Correct boundary (A produces scores, C applies as CONSTRUCTION tilt, gated by `phase1_composition_enabled` default-False at config/portfolio_settings.json:21). Not in either registry. `screens/industry_momentum.py` = true orphan (only a script imports it).
- **PortfolioComposer in alpha_engine is NOT a boundary violation** — `alpha_engine.py:76-77` imports it FROM `engines.engine_c_portfolio.composer` (F4 close held); prod method `weighted_sum` = no-op.
