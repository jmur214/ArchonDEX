---
name: pattern-bitwise-proof-scope
description: "Canon matched bitwise" rulings — always check WHICH cell/window the proof covers; directors over-extend single-cell canon proofs to whole task corpora days after T-126 taught the opposite
metadata:
  type: project
---

Pattern (found 2026-06-10, T-133 no-rerun ruling audit): a bitwise canon match is scoped to one (config, window, anchor) triple. The T-133 ruling "A's T-120 OFF-canon matched baseline bitwise -> no reruns needed for A's May-12->Jun-10 corpus" extended a single 2024-cell proof (T-120 audit table explicitly covers ONLY 2024) to cover 2022 cells whose results (sleeve-HURTS-in-crisis) set T-128's negative prior. This contradicts the methodology lesson the same arc had just produced (T-126: "single-cell canon proof leaks across windows"; T-131: anchor benignity was "luck, not structure").

**Why:** the within-pair A/B direction usually survives (same anchor both arms), so the over-extension rarely bites — but "assessed, no measurement" rulings should be labeled as such, and the free verification (content-diff of archived anchors, classify deltas as load-inert-or-not) was never recorded as done.

**How to apply:** when auditing any "no re-verification needed" ruling, demand: (1) the exact cell/window the bitwise proof covers, quoted; (2) whether the comparator canon was produced under a canonical seed by a DIFFERENT worktree (T-099/agent-B origin of b6137649 saved T-120's 2024 proof from circularity — check this chain every time); (3) whether the free content-diff was actually performed. Related: [[pattern-flag-vs-path-disconnect]].
